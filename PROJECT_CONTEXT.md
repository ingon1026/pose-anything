# 텍스트 기반 컨베이어 물체 인식과 3D 기하 자세 추정

## 프로젝트 목표

컨베이어 벨트를 지나가는 여러 물체 중에서 사용자가 텍스트로 지정한 물체를 찾고, RGB-D 영상으로 그 물체의 위치와 대략적인 형태를 계산하는 프로젝트다.

예를 들어 사용자가 `물통`, `마우스`, `필통` 같은 이름을 입력하면 화면에서 해당 물체를 찾아 분할하고, 다음 정보를 얻는 것이 목표다.

- 물체의 검출 결과와 마스크
- 카메라 기준 3D 중심 좌표 `(x, y, z)`
- 카메라와 물체 사이의 깊이 또는 거리
- 물체의 대략적인 폭·깊이·높이
- 물체 형상에서 계산한 3개 축과 Roll·Pitch·Yaw
- RGB 화면 위에 표시되는 3D 박스와 축
- 움직이는 컨베이어에서 같은 물체를 이어서 추적할 수 있는 ID

이 프로젝트는 로봇팔의 관절 경로, 충돌 회피, 실제 그립 동작을 계산하는 단계까지 포함하지 않는다. 비전 시스템이 로봇 또는 다른 프로그램에 넘겨줄 수 있는 물체 인식 결과와 3D 기하 정보를 만드는 데 초점이 있다.

## 생각하고 있는 기술 구성

- **ROS 2 Jazzy**: 카메라, 인식, 3D 계산, 시각화 사이의 데이터 흐름
- **Intel RealSense D455**: 컬러 영상, 컬러에 정렬된 Depth, CameraInfo
- **SAM 3.1**: 텍스트로 지정한 물체의 open-vocabulary 검출·분할·영상 추적
- **Open3D**: 마스크 영역의 Depth를 포인트 클라우드로 변환하고 OBB(Oriented Bounding Box)를 계산
- **OpenCV / RViz**: 마스크, 중심점, 3D 박스, 로컬 축을 화면에 표시
- **Docker / Docker Compose**: NVIDIA GPU와 RealSense를 포함한 실행 환경 배포

전체 흐름은 대략 다음과 같은 형태를 생각하고 있다.

`텍스트 입력 → 물체 검출·분할·추적 → 마스크와 Depth 결합 → 물체 포인트 클라우드 → OBB와 중심·크기·회전 계산 → ROS 2 메시지와 화면 표시`

## 현재 개발 환경

- WSL2 Ubuntu 24.04
- NVIDIA RTX 4070 Ti 12GB

초기 개발과 검증은 PC의 RTX 4070 Ti 환경이 기준이며, 작은 엣지 장치 최적화는 기본 기능이 확인된 뒤 별도로 검토할 수 있다.

## 준비된 테스트 데이터

### test2 rosbag

- 컨베이어 벨트는 정지된 상태
- 화면에 여러 종류의 물체가 존재
- 텍스트로 입력한 물체를 올바르게 찾고 분할하는지 확인하기 위한 데이터
- 물통, 마우스, 필통처럼 서로 다른 프롬프트를 바꾸어가며 검출 결과, 오검출, 마스크 품질, 처리 시간을 살펴보는 용도

### test3 rosbag

- 컨베이어 벨트가 움직이는 상태
- 물체 3개가 존재
- 텍스트 검출·분할뿐 아니라 물체 추적과 3D 축 계산을 함께 확인하기 위한 데이터
- 프레임이 바뀌어도 물체 ID가 유지되는지, 위치·크기·각도 값이 얼마나 안정적인지 살펴보는 용도

rosbag에는 D455의 RGB, 컬러에 정렬된 Depth, CameraInfo가 들어 있을 것으로 예상한다. 실제 토픽 이름, 저장 형식, 프레임 ID, 타임스탬프 구성은 bag 정보를 보고 현재 데이터에 맞추면 된다.

## 원하는 결과 형태

실행 중 사용자가 물체 이름을 입력하면 해당 물체의 결과가 화면과 ROS 2 데이터로 함께 보이는 형태를 원한다.

화면에서는 다음과 같은 내용을 확인하고 싶다.

- 입력한 물체 이름과 검출 신뢰도
- 물체 마스크와 추적 ID
- 3D 중심점과 거리
- 폭·깊이·높이
- Roll·Pitch·Yaw 또는 quaternion
- RGB 영상에 투영한 3D 박스와 X·Y·Z 축

다른 노드가 사용할 수 있는 데이터 형태로는 `PoseStamped`, 크기 벡터, 물체 ID, 분류 이름, 신뢰도 등을 생각하고 있다. RViz Marker와 디버그 영상도 함께 있으면 실제 값과 화면 결과를 비교하기 좋다. 실험 결과는 CSV나 JSON으로 저장해 위치·크기·각도의 프레임별 변화를 확인할 수 있으면 좋다.

최종적으로는 rosbag 재생 또는 D455 실시간 입력을 선택해 실행할 수 있고, ROS 2 launch와 Docker 환경에서 같은 파이프라인을 재현할 수 있는 상태를 기대한다.

## 6D pose에 대한 의미

CAD 모델이나 물체별 기준 자세를 사용하지 않으므로 여기서 얻는 자세는 물체의 의미적인 정면을 찾는 canonical pose라기보다, 관측된 3D 형상에 맞춘 **기하학적 OBB 자세**에 가깝다.

따라서 물통처럼 회전 대칭에 가까운 물체는 Yaw가 명확하지 않을 수 있고, 마우스나 필통도 보이는 포인트의 분포에 따라 축이 90도 또는 180도 뒤집힐 수 있다. 움직이는 영상에서는 이전 프레임의 축과 비교해 방향을 이어 주는 안정화가 결과 품질에 중요하다.

test3만으로는 실제 정답 자세와의 절대 오차보다 반복성과 시간적 안정성을 주로 평가할 수 있다. 위치 궤적의 흔들림, 크기 변화율, RPY 변화량, 축 뒤집힘 횟수, 추적 끊김, 처리 속도 등이 주요 확인 대상이다. 나중에 절대 각도 오차가 필요하면 ArUco 마커, 각도 지그 또는 알려진 자세로 촬영한 별도 데이터를 기준값으로 추가할 수 있다.

## 이 프로젝트에서 기대하는 완료 모습

test2에서는 텍스트로 지정한 여러 물체가 제대로 검출·분할되는 모습을 확인하고, test3에서는 움직이는 대상의 추적 ID와 3D 중심·크기·축·자세 값이 프레임마다 출력되는 모습을 확인하고 싶다.

결과적으로 `물통` 같은 텍스트 입력 하나로 대상 물체가 선택되고, RGB 화면의 3D 박스 및 축과 함께 ROS 2 토픽에서 위치·거리·크기·자세 데이터를 받을 수 있는 시스템이 이 프로젝트의 목표다.
