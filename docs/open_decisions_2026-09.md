# 로봇 요구사항이 있어야 정해지는 결정 3건 (9월 WBS 7.5 용)

2026-08-21~24 에 세 번 같은 벽에 부딪혀 판단을 미뤘다 — **그리퍼가 무엇을
얼마나 허용하는지 모르면 어느 쪽이 옳은지 정할 수 없다.** 셋을 한자리에 모은다.

**이 문서는 결정문이 아니라 결정을 돕는 표다.** 실측은 전부 끝나 있고, 로봇
쪽 답만 채우면 값이 읽힌다. 숫자는 원문 문서에서 인용했으며 새로 만들지 않았다.

| | 결정 대상 | 현재 값 | 원문 |
|---|---|---|---|
| ① | `PUB_POS_STD_MAX` | `0.02` (m) — 주석에 "로봇 사양 확정 전 잠정" | `publish_gap_2026-08-24.md` |
| ② | 화면 경계에 상시 걸친 물체의 발행 정책 | 정책 없음(그냥 발행) | `border_margin_2026-08-21.md`, `truncation_unify_2026-08-24.md` |
| ③ | `image_size` 672 vs 1008 | Isaac 프리셋 `672` | `image_size_2026-08-21.md` |

---

## 1. 로봇 쪽에 물어볼 질문

답이 **숫자나 예/아니오로 오고, 그 답이 곧바로 값을 정하는** 형태로만 적었다.

| # | 질문 | 무엇이 정해지나 |
|---|---|---|
| **Q1** | 파지 성공에 필요한 **물체 중심 좌표 오차 허용치**는 몇 mm 인가? | ① 문턱, ③ |
| **Q2** | 파지 시도 중 **실패를 허용하는 비율**은? (100회 중 몇 회) | ① 문턱 |
| **Q3** | 로봇이 **치수(파지 폭)를 쓰는가**, 위치만 쓰는가? | ② 갈래 |
| **Q4** | 잘못된 치수로 파지를 시도하면 **무슨 일이 일어나는가**? (충돌 / 놓침 / 재시도 가능) | ② 갈래 |
| **Q5** | 로봇 쪽이 `pose.covariance` 를 **읽고 판단할 수 있는가**? | ② 갈래 |
| **Q6** | 인식 결과가 **최소 몇 Hz** 로 필요한가? | ③ |
| **Q7** | 파지 대상이 **컨베이어에서 정지하는가**, 이동 중 잡는가? | ③, ② 우선순위 |

> **Q1 주의**: 아래 ① 표는 "30mm 초과 비율" 로 되어 있다. 허용치가 30mm 근처면
> 그대로 읽히지만, 예컨대 ±10mm 라면 **그 기준으로 다시 재야 한다**(원본 CSV 가
> 있으므로 재측정은 가능, 재실행 불필요).

---

## 2. 항목별 의사결정 표

### ① `PUB_POS_STD_MAX` — σ 문턱

발행 필터의 위치 불확실성 σ 가 실제 오차를 잘 예측한다는 것이 실측됐다
(test5, 정지 씬):

| σ 구간 | 오차 중앙 | 30mm 초과 |
|---|---|---|
| 0~5mm | 1~9mm | **2~8%** |
| 5~10mm | 5~28mm | 13~44% |
| 10~20mm | 4~46mm | **16~76%** |
| 20~50mm | 15~48mm | 35~66% |
| 50mm~ | 40~380mm | 70~100% |

**Q1·Q2 답 → 값**:

| Q2(허용 실패율) | 고를 문턱 | 근거 | 대가 |
|---|---|---|---|
| ~10% 이하 | **σ < 5mm** (`0.005`) | 30mm 초과 2~8% | 커버리지 **−40~47%** |
| ~20% 이하 | **σ < 10mm** (`0.01`) | 30mm 초과 4~18% | 커버리지 **−17~24%** |
| 그 이상 허용 | 현행 `0.02` 유지 | 30mm 초과 6~28% | — |

물체별 교환비(σ 조건만 적용):

| 라벨 | σ<20mm (현행) | σ<10mm | σ<5mm |
|---|---|---|---|
| black bag | 891f / 18mm / 28% | 740f / 14mm / 18% | 536f / 9mm / **8%** |
| keyboard | 684f / 2mm / 16% | 518f / 1mm / 12% | 365f / 1mm / **7%** |
| book | 750f / 1mm / 6% | 628f / 1mm / 4% | 543f / 1mm / 2% |
| gray notebook | 1005f / 1mm / 7% | 928f / 1mm / 5% | 779f / 1mm / 2% |

문제 물체는 **black bag(28%)과 keyboard(16%)** 다. book·notebook 은 현행에서도
이미 중앙 1mm 라 문턱을 조여도 얻는 게 적다. black bag 은 별도로 **센서 한계**로
결론난 물체다(`black_bag_limit_2026-08-21.md`).

### ② 경계 물체 발행 정책

잘린 물체는 치수를 **22~29% 짧게** 보고한다(길이 156mm / **정답 200mm**,
두께 43mm / **정답 54.5mm**). 한때 두께를 "다른 블록 57~58mm" 과 비교해 적었는데
그건 정답이 아니라 **우리 측정값**이다 — 씬 정답은 모델 55.0mm, 벨트 위 가시
높이 54.5mm 다(`docs/isaac_sim_connection_2026-08-13.md` :98-118). 정답 대비로는
두께가 **−11.5mm(−21%)** 다. 관측 층에서 고치는 시도는 **되돌렸다** — 절단 관측을
버리면 정상이던 트랙이 **2,278 → 356 프레임**으로 무너진다
(`truncation_unify_2026-08-24.md`).

**Q3·Q4·Q5 답 → 갈래**:

| Q3 치수 사용 | Q5 covariance 판독 | Q4 실패 비용 | 고를 갈래 |
|---|---|---|---|
| 안 씀(위치만) | — | — | **1. 치수만 불신, 위치는 발행** |
| 씀 | **가능** | — | **2. covariance 로 알리고 소비자 판단** |
| 씀 | 불가 | 큼(충돌) | **3. 발행 제외** |
| 씀 | 불가 | 작음(재시도 가능) | 1 또는 3 — 운용 판단 |

> **갈래 2 는 이미 기계적으로 준비돼 있다.** `perception_node` 가 융합 필터의
> `pos_var` 를 `pose.covariance` 대각(0/7/14)에 실어 발행한다. 즉 갈래 2 를 고르면
> **추가 구현은 "절단 중임을 covariance 에 반영" 하나뿐**이고, 채널은 이미 있다.

### ③ `image_size` 672 vs 1008

> **⚠ 이 측정의 빌드가 이후 일부 폐기됐다 (2026-08-24 확인).**
> 측정 시각 17:04 는 다른 도구(Codex)가 16:06~16:10 에 리빌드한 상태였고,
> 그 작업 일부가 커밋되지 않은 채 폐기됐다. 갈라보면:
>
> | 측정 당시 있던 것 | 폐기 후 |
> |---|---|
> | `sync_queue_size=1` (Isaac 프리셋) | **남음** — 노드가 선언한다 |
> | 지연 프레임 드롭(`pipeline.py`) | **남음** — 커밋됐다 |
> | **구독 QoS BEST_EFFORT / depth 1** | **사라짐** — 지금 노드의 `Subscriber` 에 `qos_profile` 인자가 없다 |
> | `input_health` / `pose_covariance` | 사라짐 |
>
> 즉 **4.98 / 2.68 Hz 는 지금 출하 코드의 값이 아닐 수 있다.** 큐 깊이는
> 동기화기 쪽(`sync_queue_size`)만 남고 **구독자 쪽 깊이 제한이 빠졌으므로**,
> 밀린 프레임이 다시 쌓일 여지가 생긴다. −46.1% 라는 **비율**은 양쪽에 같은
> 조건이 걸려 있어 상대 비교로는 여전히 읽히지만, **절대 Hz 를 인용할 때는
> 이 조건을 같이 적을 것.** 재측정이 필요한 항목으로 되돌린다.
>
> **재측정 완료 (2026-08-24 저녁, 현재 HEAD). 이 조건은 닫혔다.**
> 폐기 전 빌드의 값과 나란히:
>
> | | 폐기된 빌드 (17:04) | **현재 HEAD** |
> |---|---|---|
> | 672 | 4.98 Hz | **4.72 Hz** |
> | 1008 | 2.68 Hz | **2.72 Hz** |
> | 격차 | −46.1% | **−42.4%** |
> | ④ 피크 672 / 1008 | 8,691 / 8,869 MiB | **9,218 / 9,585 MiB** |
> | 12GB 대비 여유(1008) | 3,413 MiB | **2,697 MiB** |
>
> **### ⚠ [2026-08-25] 라벨이 뒤집힌다 — 출하 조건은 왼쪽 열이다**
> Codex 작업이 커밋되면서 **빠졌던 구독 QoS 가 되살아난다**(`sync_queue_size` 5→1,
> **BEST_EFFORT / depth=1**). 즉 위 표에서 **"폐기된 빌드(17:04)" 열이 출하
> 조건에 가깝고, "현재 HEAD" 열이 낡는다** — 출하 조건은 **4.98 / 2.68 Hz
> (−46.1%)** 다. **재측정이 아니라 라벨 정정이고**, 17:04 빌드가 지금 트리와
> **바이트 동일하지 않으니 "가장 가까운 빌드" 로만 읽을 것**(정확한 재측정은
> 안 했다). **672 결정은 −42% 든 −46% 든 바뀌지 않는다.**
> **아래 문단과 이 문서의 −42.4% 표기는 전부 이 정정을 받는다.**
>
> 구독 QoS(BEST_EFFORT/depth 1)가 빠지면서 **672 만 5% 손해**를 봤고(4.98 →
> 4.72), 1008 은 측정 잡음 범위에서 그대로다 — 예측한 방향이다. 격차가
> 좁아졌지만 여전히 40%대라 **`image_size=672` 결정은 바뀌지 않는다.**
> VRAM 도 1008 에서 2.7GB 가 남아 여전히 판단 근거가 아니다.
>
> 전제 검사 셋 다 통과(Publisher count 1, 입력 25~29 Hz, 검출 실제 발행).
> ② Isaac 단독이 6,794 → 7,123 MiB 로 오른 것은 Isaac 장시간 구동 누적으로
> 보이며 우리 코드 변화가 아니다.


라이브 실측(`scripts/measure_vram.sh`, **전제 검사 통과** — Publisher count 1,
입력 26.5~29.0 Hz, 검출 실제 발행 확인. `sync_queue_size=1` 이후 빌드):

| | 672 | 1008 |
|---|---|---|
| 검출 발행률 (**QoS 가 빠졌던 빌드**. 출하 조건은 4.98 / 2.68, −46.1% — 위 라벨 정정) | 4.72 Hz | 2.72 Hz (−42.4%) |
| 큰 물체 **폭** (정답 55.0) | **46.8mm (−8.2)** | 49.8mm (**−5.2**) ← 더 정확 |
| 큰 물체 두께 (정답 **54.5** = 벨트 위 가시 높이) | 57.8mm (+3.3) | 57.7mm (+3.2) — **구분되지 않는다** |
| 소물체(39px) | 소실 — **어휘로 회피 가능** | 생존 |
| VRAM 동시 피크 | 8,691 MiB | 8,869 MiB (**여유 3,591 / 3,413 MiB** — 올리는 비용 +178 MiB) |

> **VRAM 행은 RTX 4070 Ti 12,282 MiB · WSL2 값이다** — 이 문서의 `12GB 대비 여유`
> 표(위 인용 블록 포함)도 같다. **여유는 총량이 분모라, 통합 메모리 기계에서는 값이
> 달라지는 게 아니라 계산이 성립하지 않는다**(DGX Spark/GB10 은 `memory.total` 이
> `[N/A]`). 동시 피크·`+178 MiB` 같은 절대량은 양은 정의되고 값만 바뀐다.
> → [shared_server](shared_server_2026-08-31.md) §1

> **⚠ 측정 조건 — 둘 중 하나는 닫혔고 하나는 열려 있다.**
>
> **(1) ~~`sync_queue_size` 변경 이전 값~~ → 닫힘(재측정으로 확인).**
> 2026-08-24 16:06~16:09 에 color/depth 가 `BEST_EFFORT`/`KEEP_LAST`/depth 1,
> 동기화 큐 1, 늦은 프레임 폐기로 바뀌었고, **그 이후로 라이브 재측정했다.**
> 결론은 바뀌지 않았다. 다만 그 측정(4.976 / 2.683, −46.1%)의 빌드가 이후 일부
> 폐기돼 **현재 HEAD 에서 다시 쟀다 — 4.72 / 2.72 Hz (−42.4%)**.
> **[2026-08-25] 그 HEAD 는 QoS 가 빠져 있던 빌드다 — 출하 조건은 4.98 / 2.68
> (−46.1%) 쪽이다(위 라벨 정정).** 구독 QoS 가
> 빠지면서 672 만 5% 손해를 봤고 1008 은 그대로다. **격차는 여전히 40%대라
> 결정이 바뀌지 않는다. 이제 조건부가 아니다.**
>
> **(2) 실행 로그에 cpp extension 경고가 뜨는 환경이다 — 여전히 열린 조건이다.
> 단, 이 경고를 내는 것은 `torchao` 이지 SAM3 가 아니다.** 환경이 `torch 2.10.0+cu128` 이라
> `torchao 0.17.0`(요구 `>= 2.11.0`)이 prebuilt `_C*.so` 를 건너뛴다
> (`torchao/__init__.py` 74~80행). **빠지는 것은 torchao 의 양자화 커널이고,
> SAM3 는 `transformers` 로 bf16 로 돈다** — `sam3_detector.py` 에
> `quantization_config` 도 없고 `transformers/models/sam3/` 에 `torchao` 참조도
> 없다(grep 0건). **이 경고가 속도에 영향이 있는지 자체가 미확인이고, 없을
> 가능성이 크다.**
>
> **인수인계 문서와 다르다.** `roboworld_vision_audit_handoff.md` 155~157행은
> 이걸 "SAM3 C++ extension 이 `torch >= 2.11.0` 을 요구한다" 로 적었는데
> **부정확하고, 그대로 읽으면 torch 업그레이드가 급한 일로 오해된다.** 급하지
> 않다 — SAM3 추론 경로가 torchao 를 쓴다는 증거가 없다.
>
> 그럼에도 조건으로 적어두는 이유는 **672 의 근거가 이 속도 하나뿐이라**
> (VRAM 은 아래대로 기각됐다) 측정 환경이 바뀌면 한 번은 다시 재야 하기
> 때문이다. 다만 **672 가 −42.4%(출하 조건이면 −46.1%)로 여전히 크게 유리하므로 이 조건이 결정을 뒤집을
> 여지는 아침보다 줄었다.**
>
> 치수 세 행(폭·두께·소물체)은 조건이 다르다 — `isaac_belt_moving` bag 의
> **오프라인 재생**(08-24 14:35~15:35, 오늘 코드 변경 이전)이지만 OBB·두께를
> 만드는 `geometry.py` 는 오늘 안 바뀌었으므로(마지막 변경 `4a3384a`,
> 08-21 17:06) **치수 결론은 유효하다.** 자세한 구분은
> `image_size_2026-08-21.md` ③④의 "측정 조건" 절.

**VRAM 은 판단 근거가 아니다** — 유효 측정으로 더 분명해졌다. 1008 에서도
**3,413 MiB** 가 남고 올리는 비용이 **+178 MiB** 뿐이다. 실제 교환은
**속도(4.976↔2.683 Hz — **구독 QoS 있는, 출하 조건에 가장 가까운 빌드**) 대 폭 3.0mm** 다. 두께는 해상도와 무관하므로 교환에
들어가지 않는다. 정답 치수를 씬 USD 에서 확보해(**200 × 55 × 55 mm**)
**1008 이 폭에서 3.0mm 더 정확함이 확정됐다**.

> **단, 1008 로 올려도 폭은 −5.2mm 남는다.** ~~그 몫은 `erode_px=3` 이 만드는
> 것으로 보이므로 **폭 정확도가 필요하면 해상도를 올리기 전에 침식 보정을 먼저
> 볼 것** — 속도 비용이 0 이다.~~
> **[2026-08-25 정정 — 침식 보정을 보지 말 것.]** 0/3/5 A/B 를 다시 뽑았고
> **침식의 폭 기여는 −0.00mm** 였다(길이만 −2.14). depth 밴드 클립이 먼저 같은
> 화소를 지우고 있었다. −5.2mm 의 주범은 **`obb_on_plane` 의 화소중심 규약
> −2.93mm** 다 — `_backproject` 가 정수 화소를 역투영하고 `obb_on_plane` 이 그
> **중심**에 `minAreaRect` 를 걸어 n 화소를 n−1 로 잰다. **다만 일률 `+1px` 은
> 틀린다**(오차가 각도 의존: 0° −3.03 / 12° −0.17 / 31° −0.48 / 45° −2.04mm).
> `docs/README.md` §3-1 · §4 "별건으로 남긴 것" 참고
> 두께도 양쪽 다 **+3.2mm 과대**인데, **씬 정답으로 완전히 분해된다** —
> **평면 +2.45mm(낮게 앉음), 상면 −0.5mm.**
> **[2026-08-25] 이 값은 `dist_thresh=6mm` 조건이다.** `47ad9cb` 이후
> **Isaac 은 3mm 가지**라 평면 **+1.34** · 두께 **+1.97mm** (**−38%**) 다.
> test2/test4 는 여전히 6mm 폴백이라 그 씬에서는 위 값이 유효하다. 상면 측정은 사실상 정확하고
> 오차는 거의 전부 평면에서 온다. **해상도로는 어느 쪽도 안 풀린다.**
>
> **두께의 정답은 55.0 이 아니라 54.5mm 다** — 우리가 재는 것은 `평면 − 상면`
> 이라 벨트 위로 나온 높이이고, 블록이 벨트에 0.5mm 잠겨 있다. 모델 치수
> 55.0mm 와 구분할 것. 씬 정답의 출처는
> `docs/isaac_sim_connection_2026-08-13.md` 의 "씬 기하" 절(:98-118, 08-13 자)이고,
> 경위는 `docs/belt_plane_2026-08-21.md` 맨 위 정정 블록이다.
>
> **672 와 1008 의 두께 0.1mm 차이를 "같다" 로 읽지 말 것.** 평면은 실행당 한 번
> 맞춰 캐시되므로(`pipeline.py` `_plane_tried`) 그 오차가 프레임 간 std 에
> 안 나타나고, **이 씬(Isaac)의 실행 간 변동 폭이 ±0.40mm**(repeats=21 실측)라
> 0.1mm 는 **구분되지 않는다.** 단 **길이 0.8mm 에 같은 논리를 쓰면 안 된다** —
> 평면 변동은 두께에만 걸리고 풋프린트에는 안 걸린다(아래 정정 참고).
> **폭 3.0mm 만 그 변동보다 확실히 크다** — 이 결정이 폭 하나에 걸려 있는 이유다.

> **정정(2026-08-24)**: 이 표는 원래 "큰 물체 치수 −18% (46.8mm / 정답 57.3)"
> 이었다. **오측이었다** — 46.8mm 는 두께가 아니라 폭이고, 그것을 두께 정답과
> 비교한 것이다. 양쪽 500프레임 동일조건 재측정으로 정정했다. 경위와 재발 방지는
> `docs/image_size_2026-08-21.md` ④.

**Q1·Q6 답 → 값**:

| Q6(필요 발행률) | Q1(치수 허용) | 고를 값 |
|---|---|---|
| > **2.68** Hz 필요 | — | **672** (선택지 없음) |
| ≤ **2.68** Hz 로 충분 | 폭 3mm 과소 불가 | **1008** |
| ≤ **2.68** Hz 로 충분 | 폭 3mm 과소 허용 | 672 (속도 여유를 남긴다) |

> **[2026-08-25] 경계값을 2.72 → 2.68 Hz 로 되돌렸다.** 2.72 는 구독 QoS 가
> 빠져 있던 빌드의 1008 값이고, 그 QoS 가 커밋으로 되살아나 **출하 조건의 1008
> 은 2.68 Hz** 다(위 라벨 정정). 바로 아래 문단이 이미 **2.68 을 "라이브 유효
> 측정값"** 이라 부르고 있어 표만 어긋나 있었다. **갈래 자체는 안 바뀐다** —
> 두 값 차이가 0.04 Hz 다.

> **경계값 2.68 Hz 는 라이브 유효 측정값이다**(전제 검사 통과, `sync_queue_size=1`
> 이후 빌드). 큐 조건은 닫혔으므로 Q6 답을 그대로 읽어도 된다. **남은 조건은
> `torchao` 경고 하나**이고, 그게 속도와 관계가 있는지는 미확인이다 — 다만
> 672 가 −42.4%(출하 조건이면 **−46.1%**)로 유리해 이 조건이 표를 뒤집을 여지는 작다. Q6 답이
> **2.68 Hz 를 넘으면 672 밖에 없다.**

> 폭 3.0mm 차이는 **어휘로 해결되지 않는다.** 소물체 소실만 어휘로 회피된다.
> 따라서 Q1 은 "18% 오차를 감당하나" 가 아니라 **"파지 폭이 3mm 작게 보고돼도
> 그리퍼가 잡나"** 로 물어야 한다 — 훨씬 좁은 질문이고, 대개 답이 "예" 다.
> 덧붙여 정답 치수는 확보됐다(200 × 55 × 55mm). 이제 질문은 "1008 로 올려
> 폭 오차를 −8.2 → −5.2mm 로 줄이는 데 발행률 **46.1%** 를 낼 것인가" 이고,
> **침식 보정을 먼저 시도하면 그 지출 없이 더 줄 수도 있다.**
>
> **[2026-08-25] 아래 블록은 기각된 기록이다 — 침식 보정은 대안이 아니다.**
> 종단 A/B 가 다시 뽑혔고(하네스 수정 완료) **침식의 폭 기여는 −0.00mm** 다.
> 아래 "재측정은 중단 상태" 도 더 이상 참이 아니다. 주범은 **화소중심 규약**
> (위 정정 블록). 원문은 그대로 남긴다.
>
> ~~**단, 침식 보정은 실증된 후보이지 확정된 대안이 아니다 — 실제 기여분은
> 미측정이다.** 갈린 것과 안 갈린 것:~~
> - **3×3 침식이 축당 2px 를 깎는 것은 실증됐다** — CPU 합성 마스크
>   20×20px 에서 폭 63.3 → 56.7mm. 이 씬은 1px = 2.93mm 이므로 **5.9mm** 다
> - **`erode_px=1` 은 무동작이다** — 1×1 구조요소라 아무것도 안 깎인다
>   (`geometry.py:47-48`). **A/B 를 0/1/3/5 로 짜면 0 과 1 이 같은 값이 나와
>   "침식은 무관하다" 로 오독하게 된다.** 0/3/5 로 짤 것
> - **그 5.9mm 가 실측 부족분(−5.2mm)을 얼마나 설명하는지는 미확인이다.**
>   종단 A/B 를 시도했으나 계측 하네스 결함으로 무효가 됐다 —
>   `scripts/run_offline.py:184-188` 이 `--raw` 일 때 `roboworld_perception.geometry`
>   에서 함수를 **따로 import** 해서, 파이프라인 쪽 `erode_px` 변경이 raw CSV
>   경로에 닿지 않는다. **재측정은 중단 상태이고, 하네스를 먼저 고쳐야 한다**
>
> 즉 지금 확실한 것은 **"1008 로 올리면 폭이 −8.2 → −5.2mm"** 하나다.

---

### Q1 답이 어떤 값으로 와도 읽히는 표 (2026-08-24 추가 측정)

기존 표가 "30mm 초과 비율" 하나뿐이라 허용치가 ±10mm 로 오면 못 읽는 문제가
있어, 원본 계측을 다시 돌려 허용치별로 만들었다 (test5, 정지 씬, 전 물체 합산).

**σ 구간별 실제 오차**

| σ 구간 | n | 오차 중앙 | >10mm | >20mm | >30mm |
|---|---|---|---|---|---|
| 0~3mm | 1591 | 1mm | **6%** | 4% | 2% |
| 3~5mm | 629 | 8mm | 45% | 29% | 10% |
| 5~10mm | 593 | 11mm | 54% | 37% | 27% |
| 10~20mm | 516 | 15mm | 61% | 46% | 39% |
| 20~50mm | 396 | 32mm | 88% | 62% | 52% |

**문턱을 정하면 초과율이 이렇게 된다** — `PUB_POS_STD_MAX` 를 그 값으로 두었을 때

| 허용치 \ 문턱 | σ<3mm | σ<5mm | σ<10mm | σ<20mm (현행) |
|---|---|---|---|---|
| **±10mm** | **6%** | 17% | 25% | 31% |
| **±20mm** | **4%** | 11% | 16% | 21% |
| **±30mm** | **2%** | 4% | 9% | 14% |

읽는 법: Q1 답이 **±10mm** 면 현행(σ<20mm)은 31% 가 초과한다 — σ<3mm 로 조여야
6% 가 된다. **±30mm** 면 현행도 14% 라 σ<10mm(9%) 정도로 충분하다.

**σ<3mm 는 커버리지 대가가 크다** — 위 n 을 보면 전체 3,725 중 1,591(43%)만
남는다. Q2(파지 실패 허용 비율)와 함께 읽어야 한다.

> 한계: **정지 씬(test5) 기준**이다. Q7 이 "이동 중 파지" 로 오면 이 표를 그대로
> 쓸 수 없다 — 이동 씬 σ↔오차는 미측정이다.

## 3. 지금 정할 수 있는 것 — 로봇 요구사항 불필요

아래는 위 질문들과 **무관하게** 지금 확정된다.

1. **`image_size` 는 patch 크기(14)의 배수여야 한다.** `800` 은 쓰면 안 된다
   (배수가 아니고, 이웃값 784=0.81 / 812=0.67 사이에서 0.34 로 꺼진다).
   현행 값들은 전부 배수다 — 1008=14×72, 896=14×64, 672=14×48
2. **소물체 대응은 해상도가 아니라 어휘다.** `cell phone` 은 672 에서도 0.77 로
   살고 **속도 비용이 0** 이다. 해상도를 올리는 길은 **46.1%** 를 내는 비싼
   방법이다(라이브 유효 측정. 아침의 "41%" 는 무효 측정 값이었다)
3. **권장 프롬프트는 지금 적용해도 안전하다** — 기하 무회귀(Δ 0.1~1.1mm),
   트랙 수 불변, 대조군 완전 무변화가 실측됐다(`datasets.md`)
4. **갈래 2 의 발행 채널은 이미 있다** — `pose.covariance` 대각이 이미 나간다

### 지금 **운용 쪽**에 물어볼 것 (로봇이 아니라 라인/기획)
- **O1. 실제 라인에 39px 급(스마트폰 크기) 소물체가 들어오는가?**
  들어오지 않으면 ③에서 소물체 항목이 통째로 빠져 "속도 vs 치수" 단일 교환이 된다
- ~~**O2. `pub_score_min` 을 실기에서 켤 계획이 있는가?**~~
  → **답 나옴(2026-08-24 실측): 실기에서 켜지 말 것. 기본값 `0.0` 현상 유지.**
  질문은 남겨둔다 — 아래가 근거다.
  - 원래 이렇게 적혀 있었다: *"켤 계획이면 어휘 교체가 **선행 조건**이다 —
    약한 프롬프트 물체가 에러 없이 94% 사라진다(753 → 45프레임)"*
  - **"선행 조건" 이 틀렸다. 선행 조건이 아니라 상충이다.** 채택 어휘
    (`"blue bar with holes"`)에서도 **정상 블록 최저 score 가 0.102** 까지
    내려간다(merge on 기준 0.112). **어휘를 강화해도 0.6 위로 안 올라간다** —
    권장 어휘 `beige notebook` 조차 p10 이 **0.59** 로 게이트 아래다
  - **유효한 문턱이 존재하지 않는다**: 정상이 0.102 까지 내려가는데 조각은
    0.645 아래로 안 내려간다. 0.102 위 어떤 값도 정상을 자르고, 0.645 아래
    어떤 값도 조각을 통과시킨다. **0.6 은 둘 다 하고 있다** — 조각 차단
    **0프레임**, 정상 블록 손실 **90프레임**
  - **근거 측정 자체는 유효했다.** 2026-08-19 43사이클에서 정상 0.840~0.945 대
    조각 0.416~0.432 로 갈렸고 그때는 0.6 이 옳았다. 그 뒤 조각을 실제로
    없앤 것이 **게이트가 아니라 `enable_merge=true` 와 조각 수정**이었고,
    그래서 **남은 저크기 트랙이 고득점이 됐다.** 즉 오측이 아니라
    **분리 변수가 score → 크기로 옮겨간 것**이다
  - 상세·불변식·재측정 방법: [`datasets.md`](datasets.md) "게이트 불변식" 절

---

## 4. 서로 얽힌 곳

```
       [어휘 교체]  ──해결──>  ③의 소물체 항목
            │
            └──✗상충──>  pub_score_min 켜기 (O2)
                 (선행조건이 아니다 — 어휘를 강화해도 정상 최저 score 가
                  0.102/0.112 라 0.6 위로 안 올라간다. 권장 어휘조차 p10 0.59)
                                   │
                                   └──같은 방향──>  ②갈래 3 (발행 제외)

       [크기]  ──분리 변수──>  조각 vs 정상 (score 가 아니다)
            조각 ext 0.073 · 실블록 0.157~0.204 — 겹침 없음

       ①σ 문턱  ─┐
                 ├─ 둘 다 Track.publishable 층. 조건이 AND 로 겹친다
       ②갈래 3  ─┘   → 같이 조이면 커버리지 손실이 곱해진다. 순서를 정할 것

       ③해상도  ──> 치수 정확도 ──> ②의 심각도
            (1008 을 고르면 잘린 물체의 치수 오차만 남고 전반 정확도는 회복)
```

**독립이 아니다. 권장 순서:**
1. 어휘 교체(지금 가능) → ③의 소물체 항목 제거.
   ~~`pub_score_min` 선행조건 해소~~ — **어휘로는 해소되지 않는다**(위 O2)
2. O1 답 → ③ 확정. **O2 는 답이 나왔다** — 실기는 `pub_score_min=0.0` 현상 유지
3. Q1~Q7 답 → ① 과 ② 를 **함께** 결정 (같은 게이트에 겹쳐 걸리므로)

> ①과 ②를 따로 정하면 커버리지 손실이 예상보다 커진다. 현재 test5 커버리지가
> 이미 51~75% 인데(`publish_gap_2026-08-24.md`), 두 조건이 곱해지면 그보다
> 낮아진다. **결정 후 반드시 회귀로 실제 커버리지를 확인할 것.**

---

## 5. 이 문서가 답하지 않는 것
- **Q1 허용치가 30mm 가 아닐 때의 ① 표** — 재측정 필요(원본 CSV 로 가능, bag 재실행 불필요)
- **이동 중 파지(Q7 이 "이동 중")일 때의 오차** — 위 σ 표는 정지 씬(test5) 기준이다.
  이동 씬 측정은 미실시
- **black bag 류(depth 흡수 표면)의 개선** — 센서 한계로 결론났다(`black_bag_limit_2026-08-21.md`).
  요구사항이 이 물체를 포함하면 조명·노출·재질 쪽 대응이 별도로 필요하다
