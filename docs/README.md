# docs 색인

이 폴더의 문서와, **각 문서가 남긴 열린 항목**을 한 곳에 모은다.

> 만든 이유: 2026-08-24 에 `datasets.md` 가 이미 적어둔
> *"weak-prompt 는 detection-threshold 문제지 occlusion logic 이 아니다 —
> 프롬프트 교체가 먼저"* 를 못 보고 다른 데를 팠다. **기록은 있는데 안 읽혔다.**
> 새 문서를 쓰면 아래 표에 한 줄 추가할 것.

---

## 1. 문서 목록

### 시뮬 연동 (Isaac Sim)
| 문서 | 요지 |
|---|---|
| [bridge_contract.md](bridge_contract.md) | Isaac→ROS2 브리지 계약. **입력이 없을 때 확인 순서가 여기 있다** — 퍼블리셔 수부터 볼 것(§3.3~3.4) |
| [isaac_sim_connection_2026-08-13.md](isaac_sim_connection_2026-08-13.md) | 연결 절차·환경변수·실행 방법. **:98-118 "씬 기하" 절가 Isaac 씬 정답이다** — 정확도를 인용하기 전에 여기부터 볼 것. :398 "밀리미터 이하" 는 **상면 기준**이라 자세 정확도가 아니다 |
| [isaac_sim_stability_2026-08-14.md](isaac_sim_stability_2026-08-14.md) | 안정성 이슈와 VRAM 내역 |

### 기하 · 자세 추정
| 문서 | 요지 |
|---|---|
| [belt_plane_2026-08-21.md](belt_plane_2026-08-21.md) | 벨트 평면 구속 OBB. 두께 붕괴·광축 편향 해소. **거기 적힌 "정답 +0.7mm / +0.4mm" 는 파생 측정 기준** — 씬 정답 대비로는 두께 **+3.2mm**(`dist_thresh=6mm`. **현행 Isaac 3mm 가지에서는 +1.97**)**, 중심 **+0.8~+1.2mm** |
| [fusion_design_2026-08.md](fusion_design_2026-08.md) | 확률 융합 필터 설계와 불변식 |
| [border_margin_2026-08-21.md](border_margin_2026-08-21.md) | 화면 밖 잘린 물체가 치수를 22% 과소보고. 원인 규명, 처방은 되돌림 |
| [truncation_unify_2026-08-24.md](truncation_unify_2026-08-24.md) | 절단 관측 처리 통일 시도 → 되돌림. **"편향은 잡음이 아니다"** |
| [subpixel_2026-08-25.md](subpixel_2026-08-25.md) | 부분화소 경계 조사. 후보 `hull_layer` 는 **무노이즈에서 5.24→2.31mm 로 이기고 flip 1% 에서 진다** — RealSense 3 bag 이 전부 초과라 **현행 유지**. 스칼라 `+1px` 은 **별도 이유로** 죽었다(각 분포 + 위상 뽑기) |

### 추적 · 가림
| 문서 | 요지 |
|---|---|
| [footprint_gate_2026-08-21.md](footprint_gate_2026-08-21.md) | 부분 가림으로 잘린 관측 기각. 가림 중 오차 137→5mm. **비대칭 판정이 핵심** |
| [publish_gap_2026-08-24.md](publish_gap_2026-08-24.md) | 발행 공백 원인 분해. `PUB_POS_STD_MAX` 에 σ↔오차 근거표 |
| [filter_divergence_2026-08-21.md](filter_divergence_2026-08-21.md) | 필터 발산 감지 조사 → 보류 |
| [black_bag_limit_2026-08-21.md](black_bag_limit_2026-08-21.md) | black bag 오염 41% 는 **센서 한계**. 코드로 못 고친다 |

### 성능 · 파라미터
| 문서 | 요지 |
|---|---|
| [image_size_2026-08-21.md](image_size_2026-08-21.md) | 672 의 대가 — 소물체 소실 / 폭 1008 보다 3mm 더 과소 / 그러나 속도는 우세. ④ 는 **오측 정정 + USD 정답 치수** |
| [detect_interval_2026-08-21.md](detect_interval_2026-08-21.md) | di 5→10 이 1.7~1.9배. 무텍스처 물체에서 손해라 기본 5 유지 |
| [tensorrt_deploy_2026-08-21.md](tensorrt_deploy_2026-08-21.md) | TensorRT 배포 영향과 철수 기준 → **보류** |

### 데이터셋
| 문서 | 요지 |
|---|---|
| [bridge_contract.md](bridge_contract.md) **§6** | **비전이 내보내는 것 — 소비자 계약.** §6.1 `/perception/status`(`diagnostic_msgs/DiagnosticArray`, **1 Hz**) 의 레벨 규칙(`input_health.classify_input_health`)·KeyValue **10키**(`prompts` 포함 — **`level` 만으로는 빈 프롬프트를 못 잡는다**)·**입력 계약 검증**. §6.2 회전 covariance 는 **측정값이 아니라 sentinel** |
| [datasets.md](datasets.md) | bag 목록·실측 사실·**권장 프롬프트**·필요한 데이터. **"게이트 불변식" 절 — 어휘를 바꾸기 전에 `publish_score_min` 과의 관계를 숫자로 확인할 것** |

### 운용 환경
| 문서 | 요지 |
|---|---|
| [shared_server_2026-08-31.md](shared_server_2026-08-31.md) | **DGX Spark 공유 서버 운용.** 통합 메모리라 **총량이 분모인 VRAM 파생값(여유·%)이 성립하지 않는다** — 저장소의 OOM 위험 모델이 거기 걸려 있다. `scripts/measure_vram.sh` 도 그대로 못 쓴다. 여럿이 쓸 때의 `ROS_DOMAIN_ID`·`pkill` 사정거리·파일 소유권·HF 캐시 공유. **§7 은 `sm_121` 경고가 성능 문제가 아님을 2×2 대조로 확인한 것**, **§8 은 arm64 에서 두께·평면 정확도가 안 달라짐을 repeats=21 분포로 확인한 것**, **§9 는 compose 로 돌릴 때 걸린 자리와 아직 안 고친 절반**, **§10 은 라이브 ROS 경로가 실제로 도는 것**(전부 2026-08-31 실측) |

### 의사결정
| 문서 | 요지 |
|---|---|
| [open_decisions_2026-09.md](open_decisions_2026-09.md) | 로봇(그리퍼) 요구사항이 결정할 항목들. Q1~Q6 와 갈래표 |

---

## 2. 결정 기록 — 지금 켜져 있는 것과 그 근거

| 설정 | 값 | 근거 |
|---|---|---|
| `use_belt_plane` | **켜짐** | [belt_plane](belt_plane_2026-08-21.md) "재평가" 절 · `ec4fc69`. 오전 판단(꺼짐)이 오후에 뒤집혔다 |
| `enable_footprint_gate` | **켜짐** | [footprint_gate](footprint_gate_2026-08-21.md) "비대칭 판정" 절 · `5a4d173` |
| `enable_merge` | **켜짐** | `5d687bf`. 실기는 `max_per_label==1` 조기 반환으로 구조적 no-op |
| `detect_interval` | **5** (Isaac 은 1) | [detect_interval](detect_interval_2026-08-21.md) |
| `image_size` | 기본 1008, **Isaac 672** | [image_size](image_size_2026-08-21.md) ③ — 근거는 VRAM 이 아니라 **속도**. **라이브 측정으로 확인.** [2026-08-25] 구독 QoS(BEST_EFFORT/depth 1)가 커밋으로 되살아나 **출하 조건은 4.98 vs 2.68 Hz (−46.1%)** 쪽이다 — 4.72/2.72(−42.4%)는 QoS 가 빠져 있던 빌드 값이다. **17:04 빌드가 지금 트리와 바이트 동일하지 않으니 "가장 가까운 빌드" 로 읽을 것**(정확한 재측정은 안 했다). 전제 검사 통과. **672 결정은 어느 쪽이든 안 바뀐다** |
| **`dist_thresh` (지지 평면)** | **3mm 우선 + 6mm 폴백** | `47ad9cb`. 3mm 로 먼저 맞추고 인라이어가 모자라면 6mm 로 내려간다. **Isaac 은 3mm 가지를 탄다** — 500프레임 종단 검증(image_size 1008, 정답 54.5): 평면 `d` **1002.45 → 1001.34**, 두께 **57.66 → 56.47**, 오차 **+3.16 → +1.97(−38%)**. 풋프린트는 불변(195.66/49.76). **test2/test4 는 여전히 6mm 폴백**([belt_plane](belt_plane_2026-08-21.md) 라이브 확인)이므로 **두께·평면 수치를 인용할 때 어느 가지인지 밝힐 것** |
| `MAX_THICKNESS` | 0.35m | `aaf9993`. 비율 규칙은 세운 원통에서 깨진다 |
| `LE_REJECT_STREAK` | 3 | `a65e273`. extent 기각 탈출을 "유한" 이 아니라 **상계**로 |
| `LATE_DROP_STREAK_MAX` | 30 | 2026-08-24. 지연 프레임 드롭이 `_last_stamp` 를 전진시키지 않아 **영구 락아웃**이 될 수 있었다 — `time_reset_required` 가 `stamp ≤ 1.0s` 를 요구하므로 `/clock` reset 후 첫 처리 프레임이 1초를 넘으면 reset 분기를 못 타고 드롭만 반복한다. **노드는 살아 있고 에러도 로그도 없다.** 재현 후 상계로 고쳤다 — `LE_REJECT_STREAK` 과 **같은 이유로 "유한" 이 아니라 "상계"** 다(§5 마지막 항목) |
| `pub_score_min` | 기본 0, **Isaac 도 0.0** | **2026-08-24 완료** — `isaac.launch.py` 의 `_PRESET` 에서 **항목을 제거**했다(base 기본값 0.0 과 같아 다시 적으면 무동작이고, 같은 파일 주석이 금지한 패턴이다). base 기본값은 `perception.launch.py`·`perception_node.py` 양쪽 모두 `0.0` 으로 확인. 근거와 **"다시 켜려면 게이트 off CSV(`--publish-score-min 0.0`)로 재측정"** 방법론은 [datasets](datasets.md) "게이트 불변식" 절 — **채택 어휘에서도 정상 블록 최저 score 가 0.102(merge on 0.112)까지 내려가고, 0.6 은 조각을 0프레임 막으면서 정상 90프레임을 잘랐다.** 어휘 강화로는 해결되지 않는다(권장 `beige notebook` 조차 p10 0.59). 그 절의 근거표·불변식·재측정 방법론은 유효하다. (여기 있던 *"datasets:58·:98 의 「현행 `isaac.launch.py` 는 0.6 을 쓴다」 서술은 낡았다"* 경고는 **철회한다** — `40f6909` 가 **같은 커밋에서** datasets 를 정정하면서 이 경고를 썼으므로 **쓰인 순간부터 안 맞았다.** 두 곳 다 취소선+정정 상태인 것을 확인했다) |
| TensorRT | **보류** | [tensorrt_deploy](tensorrt_deploy_2026-08-21.md) — vision 이 SAM3 의 57% 라 체감 상한 1.2배 |

**권장 프롬프트**([datasets.md](datasets.md)): `gray notebook`→`beige notebook`,
`book`→`manual`, `smartphone`→`cell phone`. 실물과 이름을 맞추는 것이 핵심이다.

---

## 3. 되돌린 것 — **같은 걸 다시 시도하지 않기 위해**

| 시도 | 왜 뺐나 | 출처 |
|---|---|---|
| **게이트 측정량을 면적→점 개수** | raw 시뮬레이션은 전 물체 개선인데 **실기에서 test4 black bag 이 41→49% 악화**. 시뮬레이션이 실기 회귀를 대신하지 못한다 | [black_bag_limit](black_bag_limit_2026-08-21.md) |
| **경계 판정 마진 8px** | 시드는 실제로 차단됐고 실기 오탐도 없었으나 **길이가 154mm 로 그대로**, 쓰레기 트랙 548→906 증가 | [border_margin](border_margin_2026-08-21.md) |
| **절단 관측 처리 통일**(`border` 도 폐기) | 이론은 옳으나 컨베이어는 절단이 상시라 **정상 트랙 #6 이 −84%**. 실기는 무영향 | [truncation_unify](truncation_unify_2026-08-24.md) |
| **필터 발산 감지** | 발산·가림·가속지연이 **같은 서명**이라 P 팽창이 가리개 관측을 수락시킨다. 게다가 풋프린트 게이트가 주 경로를 이미 막아 이득 소멸 | [filter_divergence](filter_divergence_2026-08-21.md) |
| **대칭 풋프린트 판정**(`\|dev\| > TAU`) | 정상 프레임을 통째로 버려 세장 물체가 4→39% 악화. **부호를 봐야 한다**(`dev < -TAU`) | [footprint_gate](footprint_gate_2026-08-21.md) |
| **연결성분 depth 클립(G안)** | Isaac 에선 이기는데 실기에서 중심 흔들림 5배 | 2026-08-20 devlog |
| **`torch.compile`** | 7% 이득에 워밍업 30초 | 2026-08-10 |

> 공통 교훈: **시뮬·합성·raw 결과로 채택을 결정하지 말 것.** 위 7건 중 3건이
> 그 함정이다. 실기 회귀를 통과하기 전까지는 "후보" 로만 부른다.

### 3-1. 측정 아티팩트로 판명된 "발견" — **계측 자체를 먼저 의심할 것**

되돌린 것과 다른 범주다. 코드는 멀쩡했고 **측정이 틀렸다.** 둘 다 그 잘못된
숫자로 원인을 지목하고 처방까지 세웠다가 뒤집혔다.

| 주장했던 "발견" | 실제 | 출처 |
|---|---|---|
| **"트랙 미매칭 0프레임 → 검출은 멀쩡, 문제는 파이프라인 내부"** | `matched` 를 `_with_frozen()` 로 셌다. frozen 트랙이 포함돼 미매칭이 0 으로 보였다. 실제 검출 실패는 book 22%, gray notebook 22% | [publish_gap](publish_gap_2026-08-24.md) |
| **"672 는 큰 물체 두께를 18% 과소 추정"** | 46.8mm 는 두께가 아니라 **폭**이었다. 두 실행에 서로 다른 축 식별 규칙을 적용했다. 두께 57.8/57.7mm(**둘 다 `dist_thresh=6mm` 값** — 현행 Isaac 3mm 가지는 56.47) 는 **"동일" 이 아니라 "구분되지 않는다"** (실행 간 평면 변동 아래) | [image_size](image_size_2026-08-21.md) ④ |
| **"④ 동시 피크 9,497 MiB — SAM3 몫이 2.2GB"** | **SAM3 가 로드만 되고 추론을 한 번도 안 한 상태**를 "동시 피크" 로 보고했다(②도 렌더 미가동). 유효 측정은 8,691 MiB / SAM3 몫 1,897. 지금은 `scripts/measure_vram.sh` 가 전제를 검사한다 — Publisher count·입력 흐름·**검출 발행 셋 다 `exit 1` 로 중단**한다(*"이 상태의 VRAM 은 활성화 몫이 빠져 무효다 — 측정을 중단한다"*). 검출 발행은 원래 경고였는데 **중단으로 승격됐다**(2026-08-24, 확인): *"값이 나와버리면 사람은 쓴다."* 검출 중단 시 `teardown()` 으로 노드를 회수하고 **② Isaac 단독 결과는 유효한 것으로 남긴다** | [image_size](image_size_2026-08-21.md) ③ |
| **"두께 정답 +0.7mm — 벨트 평면 구속이 맞다"** | 그 "정답 57.3mm" 는 우리 depth 판독에서 **파생한 측정값**이고, 진짜 씬 정답은 08-13 부터 저장소에 있었다. 파생 정답이 같은 평면 편향을 공유해 맞아떨어진 **순환 검증**이다. 씬 정답 대비 **+3.2mm** | [belt_plane](belt_plane_2026-08-21.md) 정정 블록 |
| **"무구속 OBB 가 저잡음에서 PCA 퇴화로 흔들린다"** | **잡음은 무관 변수다** — σ=0 에서 이미 만폭으로 흔들린다(장축 63.3±4.7, 참값 55). Open3D 는 점군이 아니라 **볼록껍질 정점**(2천 점 입력에서 40~53개)에 PCA 한다. 지배 변수는 **종횡비**(1.0 → yaw sd 30.5°, 3.64 → 1.4°). **이 가설의 출처는 `compute_obb` 의 docstring 이었다** — "PCA-based ... via Open3D" 라고 적혀 있어 점군 PCA 로 읽혔다 | [belt_plane](belt_plane_2026-08-21.md) |
| **"`min_inlier_ratio` 는 평면 품질과 무상관(corr +0.066)"** | **씬을 pooling 한 값이었다.** 씬 **안에서** 재면 오염된 씬에서 음의 상관 −0.562(test2) / −0.589(test4) — **인라이어가 높을수록 평면이 좋다.** 고칠 대상은 지표가 아니라 문턱 위치였고, 0.30 → **0.35** 로 올리니 test4 의 최악 통과 오차가 **598 → 34mm** 로 줄었다(**34 는 절대값이 아니다** — 이 오차 대리값의 계측기 한계는 §5 "결국 단발 게이트로 쓸 지표는 남지 않았다" 항목 참고. 자릿수 차이만 강건하다). **같은 커밋에서 rms 의 pooling 함정을 지적하면서 같은 표의 인라이어는 그대로 뒀다** | [belt_plane](belt_plane_2026-08-21.md) 정정 절 |
| **"풋프린트 폭 −4.8mm 는 SAM3 마스크가 참 실루엣보다 작아서다"** | **주범은 마스크가 아니라 "화소를 센다" 는 것이다.** 1008 폭 부족 5.2mm 가 **래스터 양자화 0.79px + 화소중심 규약 1px = 1.79px = 5.24mm** 로 닫히고 **마스크 몫은 0** 이다. 근거는 문턱이 안 들어오는 **폭축 화소 행수**(폭축이 정확히 천저라 옆면이 안 보인다): depth 연결성분 **18행** / **SAM3 마스크 20행** — 참값 18.79px 를 **1.21px 넘게 덮는다** / 마스크+z밴드 18행 / +침식 18행. **마스크는 덮고도 남고 z밴드가 깎아 depth 와 같은 값을 낸다.** `_backproject`(`geometry.py:230`)가 **정수 화소 좌표**를 역투영하고 `obb_on_plane`(`geometry.py:361-362`)이 그 **화소 중심**들에 `cv2.minAreaRect` 를 건다 — **n 화소를 덮는 영역의 중심들은 n−1 화소만 걸쳐** 축마다 정확히 1px 짧다. `fx=322.856`, z=945mm 에서 **1px = z/fx = 2.927mm** 로 실측 −2.93mm 와 해석해가 일치한다. **앞서 여기 적었던 *"z=975 군은 depth 기하만으로 폭 55.01mm — depth 는 폭을 잃지 않는다"* 는 철회한다** — 그 55.01 은 래스터 손실 **−0.64** 와 corners 부풀림 **+0.65** 가 **상쇄된 값**이고, 그 군도 참 18.21px 를 **18행**으로 잰다(z=945 는 같은 두 항이 −2.31 / +1.04 로 −1.27 을 내고 보고값 53.73 과 맞는다 — 이게 검산이다). **`corners` 분해가 준 "마스크 몫 약 1mm" 와 행수가 준 "0" 이 다른 이유도 이것**이다: `corners` 는 두 팔의 **부풀림 크기가 달라** 생긴 차를 마스크 몫으로 돌렸다. **행수 쪽이 맞다** — 문턱이 안 들어온다. **GPU 조사 한 건이 접혔다** — `pred_masks` 로짓 추출(마스크가 아예 제약이 아니라 잴 이유가 없다). **토큰 포화 가설은 안 접힌다 — 다만 형태가 바뀌었다**: 1008 에서 마스크가 안 무는데 arm 차가 3.0mm 있다. 규약·래스터를 빼면 **672 의 마스크 몫이 약 3.0mm**(`8.2 − 2.93 − 2.31`)이고 **1008 은 사실상 0** 이라, 성립하려면 **672 에서 마스크가 17행 이하로 붕괴**해야 한다 — **질적 변화**다. **비로 쓰지 말 것**: 분모가 0 으로 측정된 바로 그 양이라 `3.0×672 / (0×1008) → ∞` 이고, 이건 기전 확인이 아니라 **모수화가 무너진 것**이다. 고정 편향 모형으로는 안 나온다(과피복 1.21 원본px 이 입력px 고정이면 672 는 약 20.6행 → arm 차 0). **재는 것은 로짓도 비율도 아니라 672 의 마스크 행수 하나 — 17행 이하인가.** ⚠ **두 수는 다른 실행이다**(arm 차는 500프레임 오프라인 재생, 행수는 캡처 40 키프레임) | [image_size](image_size_2026-08-21.md) 행수 절 · 정정 블록 |
| **"각도표가 서로 다른 오차 예산을 섞었다"** | **아니다 — 같은 양의 두 통계량이다.** `−3.03mm`(합성 **위상평균**)와 `−5.21mm`(실데이터 **고정 위상**)는 다른 예산이 아니라, 0° 위상 오차가 **−1.79px(21%) / −0.79px(79%)** 둘뿐이라 **평균에서 21:79 로 섞인 것**이다(위상평균 −1.000px). 합성에 래스터 항이 빠진 게 아니었다. **이 오진이 `geometry.py` docstring 의 경고로까지 나갔다가 철회됐다** — 형제 팀 반박 → 계산 확인 → 양쪽 철회 | [subpixel](subpixel_2026-08-25.md) §6 |
| **"저장소가 냉시동에서 안 돈다 — 인수받은 사람은 한 프레임도 못 돌린다"** | **저장소 문제가 아니라 환경 오염이었다.** 클린 클론에서 bag 을 돌렸더니 CSV 가 **헤더 1줄뿐**이었고 로그에 `camera_info changed (size, intrinsics, or frame); tracker reset` 이 반복 발화했다. 발행자를 추적하니 **Isaac Sim 의 ROS 2 브리지**(`_Render_PostProcess_SDGPipeline_Replicator_01_...`, Publisher count 1)가 **노드가 구독하는 바로 그 토픽**(`/camera/camera/color/camera_info`)으로 **640×360** 을 쏘고 있었고, test4 bag 은 같은 토픽으로 **640×480** 이었다. 둘이 번갈아 도착 → 서명이 매번 바뀜 → 매번 리셋 → **출력 전량 폐기.** **증거**: `ROS_DOMAIN_ID=77` 로 격리하니 `camera_info changed` **0회**, CSV **63행**. test4 의 color camera_info 470개는 **전부 서명 1종**이라 bag 쪽은 안 변한다. **노드 동작 자체는 옳다** — 다른 캘리브레이션을 섞어 3D 로 쏘면 조용히 틀린 좌표가 나간다. 문제는 **막기만 하고 아무것도 안 나간다**는 것. 그리고 원래 결론도 틀렸다 — `run.sh` 는 인자 없으면 **라이브 RealSense 모드**라(`run.sh:22-28`, `:78`) **bag 없이 돈다** | 2026-08-25 냉시동 검증(실행). §4 "냉시동 검증" |

> 공통 교훈: **비교하기 전에 두 쪽이 같은 양을 재고 있는지 확인할 것.**
> 특히 OBB extent 는 `match_axes()` 가 축 순서를 **실행마다** 바꾼다 — 같은
> bag·같은 해상도에서 실행 길이만 달라도 `re1`/`re2` 배정이 뒤집힌다. 컬럼
> 인덱스로 대응시키면 안 되고, 축의 의미로 대응시켜야 한다: 두께 = `re3`
> (= 평면 법선 방향 `thk`, 이건 안 뒤집힌다), 길이·폭 = `max`/`min(re1, re2)`.
>
> 그리고 **"정답" 이 씬 정답인지 파생 측정인지 구분할 것.** belt_plane 이 쓰던
> "정답 57.3mm" 는 depth 판독 두 개(벨트 1002.3 − 상면 945.0)에서 나온
> **측정값**이다. 진짜 씬 정답은 **처음부터 저장소에 있었다** —
> [isaac_sim_connection](isaac_sim_connection_2026-08-13.md):98-118 (08-13 자).
> 파생 정답이 우리 측정과 같은 평면 편향(+2.45mm)을 공유해서 "정답 +0.7mm" 로
> 맞아떨어졌다 — **씬 정답 대비로는 +3.2mm 다.** 정답을 측정에서 만들면
> 검증이 순환한다.
>
> 그리고 **docstring 도 주장이다.** `compute_obb` 가 "PCA-based ... via
> Open3D" 라고 적어 둔 한 줄 때문에 "두께축 분산이 0 이면 퇴화한다" 는
> 가설이 섰고, 실제 구현(껍질 정점 PCA)에서는 성립하지 않는 이야기였다.
> **코드가 무엇을 하는지 문서가 아니라 소스에서 확인할 것** — 특히
> 외부 라이브러리를 감싼 함수에서.

---

## 4. 열린 항목 (2026-08-24 기준)

### 로봇 요구사항이 있어야 정해지는 것 — `open_decisions_2026-09.md` 참고
| 항목 | 무엇이 걸려 있나 | 출처 |
|---|---|---|
| `PUB_POS_STD_MAX` (현 0.02) | σ↔오차 표는 있다. 커버리지 40~47% 를 잃고 오염을 1/3 로 줄이는 교환 | [publish_gap](publish_gap_2026-08-24.md) |
| 경계 물체 발행 정책 | 3갈래 — 치수만 불신 / `covariance` 로 알림 / 발행 안 함 | [truncation_unify](truncation_unify_2026-08-24.md), [border_margin](border_margin_2026-08-21.md) |
| `image_size` 672 vs 1008 | 속도(**4.98 vs 2.68 Hz, −46.1%** — 구독 QoS 복원 후 출하 조건에 가장 가까운 빌드. QoS 가 빠졌던 빌드는 4.72/2.72, −42.4%. **정확한 재측정은 안 했고 결정은 어느 쪽이든 안 바뀐다**) ↔ **폭** 3.0mm(정답 55.0 대비 −8.2 vs −5.2). 두께는 무관. **이 항목은 화소중심 규약 확정(§3-1)으로 닫히지 않는다** — `_backproject` 는 **원본 640×360 래스터**에서 도므로 규약 몫 2.93mm 는 672·1008 **양쪽에 똑같이** 걸리고, 3.0mm 차를 설명하지 못한다. 그 차는 **SAM3 마스크 몫**에 남는다. **모순이 아니라 척도 의존이다**: *"마스크 몫 ≤ 약 1mm"* 는 **1008 에서 잰 값**이고 **672 의 마스크 몫은 안 쟀다** — 규약 항과 **래스터 항**(행수로 확정, 0.79px = 2.31mm)을 걷어내면 672 는 **약 3.0mm**, 1008 은 **사실상 0** 이다 — **배수로 쓸 수 없다**(분모가 0 으로 측정된 바로 그 양이다). **[2026-08-25 갱신] `1.44~6.07×` 비율 논증은 폐기됐다** — 행수 세기가 depth 래스터 항을 닫았고(0.79px) **1008 마스크 몫이 0** 이라 그 표는 0 인 분모로 나눈 값이었다. **남은 미결은 하나**: 1008 에서 폭을 정하는 것이 depth 뿐인데(`image_size` 무관) arm 차가 3.0mm 이므로, **672 에서 마스크가 17행 이하로 붕괴하는가**. 고정 편향 모형은 약 20.6행(→ arm 차 0)을 준다. **예/아니오로 갈리는 숫자 하나**이고 GPU 가 필요하다. ⚠ arm 차(500프레임 재생)와 행수(캡처 40 키프레임)는 **다른 실행**이다 | [image_size](image_size_2026-08-21.md) ③④ · "미결" 블록 |

### 값 변경 — 완료된 것도 이유와 함께 남긴다
| 항목 | 상태 | 출처 |
|---|---|---|
| **`[belt_plane]` 로그에 인라이어 비율 + 잔차 RMS 추가** | **부분 완료(2026-08-24).** `[belt_plane]` 이 이제 **`dist_thresh=Nmm` 를 찍는다** — 폴백이 어느 문턱으로 갔는지가 보인다(추정 실패 시엔 실패 문자열, `flush=True`). **남은 것: 인라이어 비율·잔차 RMS — 대기 유지.** 줄은 이미 있고 내용만 바뀌므로 **비용 0** 이고, 이걸로 13mm 씬이 눈에 보인다. **게이트를 넣기 전에 로그부터** — 실 bag 분포를 모은 뒤 문턱을 정하는 것이 순서다. `src/` 라 사용자 몫 | [belt_plane](belt_plane_2026-08-21.md) "권고 순서" |
| **`isaac.launch.py` 의 `publish_score_min=0.6` → `0.0` 권고** | **완료(2026-08-24).** 값을 다시 적는 대신 `_PRESET` 에서 **항목을 제거**했다 — base 기본값이 이미 0.0 이라 다시 적으면 무동작이고, base 가 바뀌었을 때 Isaac 만 옛 값에 남는 패턴을 그 파일 주석이 금지한다. 조각을 없앤 것은 이 게이트가 아니라 **이미 켜져 있는 `enable_merge=true`** 였으므로 0.0 으로 내려도 조각이 돌아오지 않는다. **다시 켜려면 게이트 off CSV 로 재측정할 것**(§5 "게이트를 켜고 뽑은 CSV" 항목) | [datasets](datasets.md) "게이트 불변식", §2 |

### 측정하면 답이 나오는 것
| 항목 | 출처 |
|---|---|
| ~~4.98 / 2.68 Hz 재측정~~ **완료 2026-08-24**, **[2026-08-25] 라벨 뒤집힘** — 그때의 `4.72 / 2.72 (−42.4%)` 는 **구독 QoS 가 빠져 있던** 빌드 값이고, Codex 커밋이 그 QoS 를 되살리므로 **출하 조건은 `4.98 / 2.68 (−46.1%)`** 쪽이다. **재측정이 아니라 라벨 정정**이고 17:04 빌드는 지금 트리와 바이트 동일하지 않다 — *"가장 가까운 빌드"*. 결론(672 유지) 불변 | [image_size](image_size_2026-08-21.md) ③ |
| `torchao` cpp ext 경고(torch 2.10 < 2.11)가 속도에 영향이 있는지 — **SAM3 경로가 torchao 를 쓰는 흔적이 없어 무관일 가능성이 크다.** 업그레이드 시 ③ 표를 한 번 재면 닫힌다 | [image_size](image_size_2026-08-21.md) ③ |
| ~~침식(`erode_px=3`)이 폭 과소 −5.2mm 를 얼마나 설명하는지 — **종단 A/B 는 하네스 결함으로 중단**~~ → **닫힘(2026-08-25).** 하네스를 고쳐 0/3/5 로 다시 뽑았고, **침식의 폭 기여는 −0.00mm** 다(길이는 −2.14). 축당 2px 절삭 자체는 실증됐지만 **depth 밴드 클립이 먼저 같은 화소를 지우고 있어** 폭에서는 무동작이었다. 주범은 **화소중심 규약**(§3-1, 아래 "별건" 항목) | [image_size](image_size_2026-08-21.md) ④ · "침식 단독 설명 기각" |
| **`min_inlier_ratio` 로는 test2 를 아직 못 거른다** — **문턱을 0.30 → 0.35 로 올린 뒤에도 열려 있다**(`55a335f`, 2026-08-25). 실측 통과값: isaac **0.499**(3mm) / test2 **0.371**(6mm) / test4 **0.426**(6mm) / test3 0.253(실패). **test2 의 여유가 +0.021 로 얇다** — 캡처(`--stride 15`) 기반 추정은 0.405 였는데 실제 파이프라인(`detect_interval=5`)이 고르는 첫 키프레임은 **0.371** 이었다(추정이 낙관적이었다). 실행 간 변동이 이 폭이면 test2 는 양쪽으로 간다. 그런데 test2 는 실행 간 평면 `d` 산포가 **±13mm 급**(Isaac ±0.40 / test4 ±2.98)이라 **걸러야 할 씬이다**(§5 "13mm 씬은 품질 게이트 문제"). 0.40 이상은 추가 이득 없고 **0.45 는 test2 통과를 16/16 → 7/16 으로 반토막** 낸다. **~~잔차 RMS(+0.652)·반복 불일치(+0.486)가 더 나은 후보~~ 는 기각됐다** — 그 corr 은 씬을 pooling 한 값이고(§3-1, §5 pooling 항목), rms 는 pooled AUC 0.88 인데 **최저 사분위 안에 참오차 47.32mm** 가 있었으며 `lip`(15.51mm)을 `ramp`(0.81mm)와 **0.18mm 차**로밖에 못 갈랐다. **`시드산포` 도 기각됐다**(2026-08-25) — **비용은 무관했다**(R=21 이 82~129ms, `_plane_tried` 로 실행당 1회). **판별력이 없다**: test4 의 상관은 `0.35` 가 이미 자르는 **4프레임**이 만들고(빼면 +0.812 → **+0.085**), test2 는 **같은 16프레임**에 draw 만 바꿔도 +0.008~**+0.695** 로 흔들리며, 씬 안 순위가 역전된다(half **91.1mm** 프레임이 31.7mm 프레임보다 **낮게** 나온다). **단발 게이트로 쓸 후보가 남지 않았다**(§5). **그리고 이 표의 `34mm` 는 절대값이 아니다** — 오차 대리값이 draw 마다 1.5~2배 흔들리고 과대보고가 최대 10배라 **"34mm 가 더 주는가" 는 이 계측기로 답할 수 없다** | [belt_plane](belt_plane_2026-08-21.md) 정정 절 · `55a335f` |
| test2 면내 크기 차이의 정오 — **실물 줄자 실측 필요**(책·노트북·보온병) | [belt_plane](belt_plane_2026-08-21.md) |
| 896~1008 사이 미측정. 896 이 진짜 소물체 하한인지 | [image_size](image_size_2026-08-21.md) |
| patch 정렬이 800 급락은 설명하나 전 구간은 아님 (812=0.67 < 784=0.81) | [image_size](image_size_2026-08-21.md) |
| isaac 블록이 672 에서 살아남는 이유 — 픽셀 크기만으로 예측 불가. (문서의 "33px" 은 신뢰 불가: 폭 55mm 는 fx=322.86/z=0.945 에서 18.8px 다) | [image_size](image_size_2026-08-21.md) |
| `TAU`·`ALPHA` 는 test4 book 한 사례의 고원값. 라벨된 오염 구간이 그것뿐 | [footprint_gate](footprint_gate_2026-08-21.md) |

### 냉시동 검증 — **실행으로 확인됨** (2026-08-25)

`git clone` 해서 처음부터 돌린 결과다. **추론이 아니라 실행 기록이다.**

| 단계 | 결과 |
|---|---|
| `git clone` | ✅ 22MB, 미추적 없음 |
| `colcon build` | ✅ **2.2초** |
| `pytest` (클린 클론) | ✅ **154 passed** — **그 실행 시점 값이다.** 같은 날 형제 팀이 테스트를 계속 추가해 이후 **161** 이 됐다. 개수는 조건과 같이 읽을 것 |
| `run_offline.py --bag bags/test4` | ✅ 60프레임, book 검출·추적 |
| `run.sh` (ROS 노드 경로, **도메인 격리**) | ✅ CSV **63행** |

**저장소는 냉시동에서 돈다.** 단 **도메인 격리가 전제**다 — 격리 없이 돌리면
Isaac 브리지와 토픽이 충돌해 출력이 전량 폐기된다(§3-1, §5).

**격리는 저장소 어디에도 안 걸려 있다 — 매 셸에서 손으로 걸어야 한다.**
`run.sh:45` → `scripts/ros_env.sh:28-29` 는 전송 변수 둘만 export 하고,
`perception.launch.py:20,24` · `docker-compose.yml` 의 `environment:` 도 같은 둘뿐이다
(줄번호로 안 적는다 — 그 파일은 계속 움직인다).
저장소 안의 유일한 `ROS_DOMAIN_ID` 사용처는 `test/test_perception_node.py:18`
(테스트 격리용) 하나다.

| | |
|---|---|
| **언제 충돌하나** | Isaac Sim ROS 2 브리지가 떠 있는 채로 bag 재생·라이브 실행을 할 때. 노드가 구독하는 이름이 `/camera/camera/color/image_raw` · `/camera/camera/color/camera_info` · `/camera/camera/aligned_depth_to_color/image_raw` 로 (`perception_node.py:112-115`) 브리지가 쏘는 이름과 **같다** |
| **어떻게 격리하나** | `ROS_DOMAIN_ID=77 ./run.sh bags/test4` — 노드(`run.sh:117`) · bag player(`:180`) · rviz2(`:157`) · RealSense launch(`:79`) 가 전부 이 셸의 자식이라 export 가 그대로 상속된다. `77` 은 실행으로 검증된 값이다(§3-1, §5) |
| **격리됐는지 확인** | **격리한 바로 그 셸에서** `ros2 topic info -v /camera/camera/color/camera_info` → Publisher count 가 의도한 발행자 수(bag 재생만이면 1)인가. ⚠ **다른 셸에서 치면 그 셸의 도메인을 세고 "깨끗하다" 고 나온다** — 확인 절차 자체가 무성 실패한다 |
| ⚠ **격리가 안 막는 것** | **`run.sh` 의 정리는 도메인을 안 본다.** 시작할 때 `pkill -f "roboworld_perception/perception_node"` 를 무조건 돌리므로(`run.sh:49-51`) **다른 도메인에서 돌던 인식 노드까지 죽는다** — 격리는 DDS 계층이고 pkill 은 프로세스 계층이라 서로 못 막는다. 2026-08-28 실측: Isaac 을 기본 도메인에서 돌리는 중에 `ROS_DOMAIN_ID=77 ./run.sh bags/test4` 를 띄우니 Isaac 쪽 노드가 죽었다. **동시에 쓰려면 도메인만으로는 부족하고 실행 자체를 나눠야 한다** |
| **잊으면 보이는 것** | CSV 가 **헤더 1줄**. 로그에 `camera_info changed (size, intrinsics, or frame); tracker reset` 반복. **에러도 크래시도 없다** — 노드는 살아서 매 프레임 리셋만 한다(§3-1, §5) |

⚠ **호스트에서 건 격리는 컨테이너에 안 간다.** `docker-compose.yml` 의
`environment`(`:15-22`)에 `ROS_DOMAIN_ID` 가 없다. `network_mode: host`(`:10`)
로 호스트 DDS 에 그대로 붙는데 도메인 값만 전달되지 않으므로, **호스트 셸을
격리해도 `docker compose run` 은 격리 안 된 도메인에 붙는다.** `.bashrc` 가
컨테이너에 없어서 났던 사고(`scripts/ros_env.sh:4-12`)와 같은 부류다.
**컨테이너 경로에서 격리를 걸어본 적은 아직 없다 — 미확인.**

~~**아직 검증 안 된 것 — 컨테이너 안에서 SAM3 를 띄워 본 적이 없다.**
**빌드 성공은 `SAM3 ready` 가 아니다.** Hub 이미지는 13일 전 것이라 오늘 커밋이
하나도 안 들어 있다.~~

> **갱신 (2026-08-26).** 앞의 두 문장은 더 이상 사실이 아니다. `1.2.0` 을 빌드해
> **컨테이너 안에서 `SAM3 ready` 4.1초**, `pytest` **161 passed**(로컬과 동일),
> `torch.cuda.is_available()` **True**(RTX 4070 Ti) 를 확인했고 Hub 에
> `ingon1026/pose-anything:1.2.0`(=`latest`) 로 올렸다. Hub 이미지에 오늘 커밋이
> 들어 있다.
>
> **다만 원문의 요구는 절반만 채워졌다** — 검증한 것은 **모델 로드까지**이고,
> **컨테이너 안에서 bag 을 돌려 검출이 실제로 나가는 것은 아직 안 봤다.**
> §3-1 의 VRAM 건이 같은 부류다 — *"로드만 되고 추론을 한 번도 안 한 상태"* 를
> 값으로 보고했다. 컨테이너 경로를 끝까지 신뢰하려면 **검출이 나가는 것까지**
> 봐야 하고, 그때 **도메인 격리도 같이 걸어야 한다**(바로 위 항목 — 컨테이너
> 경로의 격리는 미확인).
>
> **후속 (2026-08-28).** `1.3.0` 이 `1.2.0` 을 대체했다 — 위의 `=latest` 는 그날의
> 이야기이고 지금 `latest` 는 `1.3.0`(digest `sha256:96657bbd72b6…`, 커밋
> `ada2aa8`)이다. 컨테이너 검증도 갱신됐다: `174 passed`(호스트와 동일),
> `import yaml` 확인, CUDA True, `SAM3 ready` **9.5초** — 마지막 값은 Isaac 이
> 같은 GPU 에서 렌더 중일 때 잰 것이라 위 4.1초와 **비교 대상이 아니다.**
> **원문의 나머지 절반(컨테이너 안에서 bag 을 돌려 검출까지)은 아직 그대로
> 열려 있다** — 이번에도 안 봤다.

### 별건으로 남긴 것
| 항목 | 상태 | 출처 |
|---|---|---|
| **test4 book 가림 오염 27%** | 흐름 게이트로 못 고침. 다음 후보는 외형(색 히스토그램) 채널 | [belt_plane](belt_plane_2026-08-21.md) |
| **black bag 41%** | **센서 한계로 수용.** 개선하려면 조명·노출·재질 | [black_bag_limit](black_bag_limit_2026-08-21.md) |
| 필터 회복 국면 — 틀린 상태에서 나오는 데 2.5초 | 발산 감지는 보류 | [footprint_gate](footprint_gate_2026-08-21.md), [filter_divergence](filter_divergence_2026-08-21.md) |
| 규약 전이 재시드가 tilt 게이트 진동 시 영구 미승격 유발 가능 | 현행 데이터로는 안 드러남(폴백 2/459) | [belt_plane](belt_plane_2026-08-21.md) |
| 등속 씬에서 CV 모델이 벨트 가감속에 뒤처짐(`SIGMA_A`) | 지금 문제 아님 | [filter_divergence](filter_divergence_2026-08-21.md) |
| TensorRT | 엣지 타겟 확정 시 재개 | [tensorrt_deploy](tensorrt_deploy_2026-08-21.md) |
| **`obb_on_plane` 화소중심 규약 — 축당 1px 과소** | **2026-09-02 후보 구현 — `soft_footprint`(기본 off): Isaac 672 에서 W −8.2 → +1.05 / L −4.2 → +4.7, 실기 test4 노이즈 0.28×·desk2 1.05×, +12ms. 채택 조건은 실기 정답 — subpixel 문서 재개 절** **조사 완료, 채택 보류 — 후보는 있고 지금 조건에서 진다.** 기전: `_backproject` 가 정수 화소를 역투영하고 `obb_on_plane` 이 그 **중심**에 `minAreaRect` 를 걸어 n 화소를 n−1 로 잰다. 래스터 양자화 0.79px 과 같은 원인이라 **처방이 하나 — 부분화소 경계**(각도 인지 보정이 아니다). **후보 `hull_layer`**(변마다 `δ = pitch/2 − m`, **자유 파라미터 0**, 각도 자동 처리): 무노이즈 최악 **5.24 → 2.31mm**, 상한 **`≤ 현행 + 2.93mm`**, 구멍·가림 면역. **기각 사유: 껍질 기반 max 통계량이 산발 노이즈 화소에 부풀려진다** — 실기에서 직접 관측됐다(물체 depth ±2mm 로 조여도 마스크 밖 **14~41화소** 잔존, 투영축 끝단 두어 개가 **+2.5~2.9px**, **isaac 은 0**). 뒷받침으로 합성 대조의 **flip 교차점이 1% 아래**인데 RealSense 3 bag 이 전부 초과다(test2 2.8~3.9 / test4 1.7~8.8 / test3 1.6~6.9%; 파이프라인이 쓰는 `prod` 기준이면 **11~14%** 로 더 나쁘다). **방향은 isaac 에서만 확인됐고**(성장 0px, 참 18.79px 대비 껍질 17px span) RealSense 에서는 **어느 쪽으로도 미확인** — *"바깥이라 방향이 틀렸다"* 로 적지 말 것. 막은 것은 **z밴드가 지지면을 먹는 것**이지 회전이 아니다(부호 측정은 주축 투영 extent 라 회전에서 잘 돈다). **회수 불가 잔차 2.31mm 는 센서 성질**(18.79px 막대에 18행)이라 이진 마스크로 못 고친다. **⚠ 크기를 상수로 인용하지 말 것** — 0° 에서도 값이 **−1.79px(21%) / −0.79px(79%)** 둘뿐인 **위상 뽑기**라 관측 5.24mm 는 *"21% 뽑기가 카메라 고정으로 얼어붙은 값"* 이고, 실기 씬은 애초에 축 정렬이 아니다(test2 med **16.99°** / test4 med **18.97°**). **재개 조건: test2/test4 물리 실측 → `fp_harness.py` 의 `TRUTH` 에 bag 추가** | [subpixel](subpixel_2026-08-25.md) |

### 데이터 대기
[datasets.md](datasets.md) "Data still needed" — 전동 컨베이어 등속 bag,
이동 중 가림 bag, ArUco 씬. *(ArUco 는 Isaac 정답 좌표로 대체·종료됨)*

---

### 3-2. 원인 진술이 데이터와 맞지 않았던 것

기각된 가설(§3)이나 측정 아티팩트(§3-1)와 또 다른 범주다. **측정도 맞고
코드도 맞는데, 잔여 과제에 적어둔 "원인" 이 이 저장소 자신의 데이터와
어긋나 있었다.**

| 적혀 있던 원인 | 데이터가 말하는 것 |
|---|---|
| **"Re-ID: 복귀 매칭이 위치 기반이라 가림 중 이동하면 원래 ID 복구 실패"** | 위치 게이트는 **물체 폭의 2.5배**까지 허용한다(`rescue_buffer=2.5`, 양쪽 박스를 확장한 buffered IoU>0 — keyboard 328mm / gray notebook 355mm). 그런데 기록된 **모든** ID 분실이 **29~153mm** 에서 일어났다 — 한 자릿수 여유다. 원인은 위치가 아니라 **지속시간**이었고 `occlusion_hold`(2026-08-11)로 이미 고쳐졌다. 그 이후 test4/test5 실행 **전부에서 ID 변경 0건** |

**게다가 현행 조건에서는 재현이 원리적으로 불가능하다.** 삭제 한계는
`max_missed(5) + occlusion_hold(12)` = 18 키프레임 = **6.0초**
(`detect_interval=5`, 15fps). 벨트 46mm/s 에서 6.0초 변위는 **276mm** 로
게이트 328mm 안이다 — **타임아웃이 항상 먼저 발화해 위치 게이트가 구속
조건이 될 수 없다.** 위치가 구속이 되려면 `v > 2.5W / t_가림` — 가림 2.6초·
노트북급 물체면 **137mm/s 이상**이 필요하다.

> 공통 교훈: **잔여 과제의 "원인" 도 주장이다.** 고치기 전에 그 실패가
> 재현되는지부터 보고, 재현이 안 되면 **왜 안 되는지**(코드가 잘 처리 /
> 데이터가 없음 / **그 국면에 도달 자체가 불가능**)를 가른다. 세 번째 답이
> 실재한다 — 이 건이 그랬다.
>
> 그리고 **두 문제를 섞지 말 것.** "다음 후보는 외형(색 히스토그램) 채널" 은
> **test4 book 가림 오염 27%**(ID 는 유지된 채 마스크가 가리개로 옮겨가 pose 가
> 오염되는 문제) 얘기지 **ID 분실** 얘기가 아니다. 잔여 과제에 두 가지가
> 섞여 있었다.

---

## 5. 읽기 전 주의

- **`grep` 이 0건이라고 기록이 없는 게 아니다 — 문서 자체가 무성 실패할 수 있다.**
  `isaac_sim_stability_2026-08-14.md` 488행에 **리터럴 NUL 바이트**가 있어
  (`tr '\0' '\n'` 을 적으며 이스케이프 대신 실제 NUL 을 넣었다) `file` 이
  이 파일을 `data` 로 판정했고, **GNU grep 이 이진으로 취급해 매치를 조용히
  버렸다.** 저장소 전체 `grep -rn -i vram` 이 이 파일에서 **0건**을 냈는데,
  **이 파일이 프로세스별 VRAM 분해·OOM 실측·뷰포트 축소 기각을 들고 있는
  유일한 문서**다(§1 이 "안정성 이슈와 VRAM 내역" 으로 색인해 둔 바로 그것).
  **에러도 경고도 없다 — 그냥 안 나온다.**
  실제로 이것 때문에 2026-08-28 의 OOM 과제가 **저장소가 이미 무효 처리한
  값**(`≈2,226 MiB`)과 **합계를 단독값으로 오독한 값**(`10.3 GB`)의 합을
  전제로 출발했다. **바이트는 고쳤고**(`\0` 리터럴로 교체, 동작 동일)
  현재 저장소 `.md` 에 NUL 은 없다. 재발 확인:
  `grep -rlP '\x00' --include='*.md' .` 가 빈 결과여야 한다.
- **수치를 인용할 때 조건을 같이 적을 것.** 같은 양이 조건에 따라 크게 다르다:
  - 키프레임 시간 — `image_size` 없이는 무의미(672 vs 1008 이 2.25배)
  - **속도·발행률 — 측정 조건을 같이 적을 것: 라이브인지, 전제 검사를 통과했는지,
    동기화 큐 설정, 어느 빌드인지.** `image_size` 672 의 **유일한 근거**인
    발행률은([image_size](image_size_2026-08-21.md) ③) `scripts/measure_vram.sh`
    라이브 측정이고 **전제 검사 통과**(Publisher count 1 · 입력 26.5~29.0 Hz ·
    검출 실제 발행)인데, **어느 빌드인지로 값이 갈린다**: 구독 QoS
    (BEST_EFFORT/depth 1)가 **있으면 4.98 / 2.68(−46.1%)**, **빠지면
    4.72 / 2.72(−42.4%)** 다. Codex 커밋이 그 QoS 를 되살리므로 **출하 조건은
    전자**이고, 다만 그 측정 빌드(17:04)가 **지금 트리와 바이트 동일하지 않아
    "가장 가까운 빌드" 로만 읽어야 한다**(정확한 재측정은 안 했다).
    **672 결정은 어느 쪽이든 안 바뀐다 — 절대 Hz 를 인용할 때만 어느 빌드인지
    밝히면 된다.** **전제 검사가 왜 필요한지는 §3-1 을 볼 것** — 그게 없던 아침 측정은
    렌더가 안 돌거나 SAM3 가 추론을 안 한 상태를 값으로 보고했다.
    같은 실행 로그 첫 줄에
    `Skipping import of cpp extensions ... (found 2.10.0+cu128)` 도 찍히는데,
    **이건 `torchao 0.17.0` 이 내는 경고이고 빠지는 것은 그 양자화 커널이지
    SAM3 자체가 아니다** — SAM3 는 `transformers` 로 bf16 로 돌고
    `quantization_config` 도 없다(`transformers/models/sam3/` 에 `torchao` 참조
    0건). 속도와 관계가 있는지는 미확인이니 **"확장이 없어서 느린 값" 이라고
    적지 말 것.** 인수인계 문서(`roboworld_vision_audit_handoff.md` 155~157행)가
    이걸 "SAM3 C++ extension" 으로 적어놨는데 **부정확하고, 그대로 읽으면 torch
    업그레이드가 급한 일로 오해된다** — 급하지 않다. **이것이 속도 쪽에 남은
    유일한 열린 조건**이고, 근거가 속도 하나뿐이라 torch 를 올리면 한 번은 다시
    재야 한다.
    **DGX Spark 의 `sm_121` 경고도 torch 를 올릴 이유가 못 된다** — 같은 오해가
    한 번 더 날 자리다. **얻을 성능이 0** 이라는 것을 2×2 대조로 확인했다 →
    [shared_server](shared_server_2026-08-31.md) §7 (2026-08-31 실측)
  - 치수 정확도 — **"정답" 이 씬 정답인지 파생 측정인지 밝힐 것.** Isaac 씬
    정답은 [isaac_sim_connection](isaac_sim_connection_2026-08-13.md):98-118 에
    있다(**벨트면 1000.0 · 상면 945.5 · 물체 중심 973.0 · 벨트 위 가시 높이
    54.5mm**). 모델 치수는 200 × 55 × 55mm 인데 **두께의 비교 기준은 55.0 이
    아니라 54.5** 다 — 우리가 재는 건 `평면 − 상면` 이고 블록이 0.5mm 잠겨 있다.
    belt_plane 의 "정답 57.3mm" 는 depth 판독에서 파생된 측정값이라 정답이
    아니다. 씬 정답 대비 실측은 풋프린트 −4~8mm(과소), 두께 **+3.2mm**(과대,
    분해: 평면 +2.45 / 상면 −0.5)다 — **`dist_thresh=6mm` 값이다.** `47ad9cb`
    이후 **Isaac 은 3mm 가지**라 **+1.97mm**(분해: 평면 **+1.34** / 상면 −0.5)이고,
    **test2/test4 는 여전히 6mm 폴백**이라 그 씬에서는 +3.2 계열이 유효하다.
    **두께·평면 수치는 어느 가지인지 밝히고 인용할 것**(§2 `dist_thresh` 행). **어느 축인지도 같이 적을 것**
    — 두께는 해상도와 무관하고 폭만 3.0mm 갈린다. 그리고 raw CSV 의 `re1`/`re2`
    배정은 `match_axes` 가 실행마다 바꾼다 — 컬럼 인덱스로 비교하면 안 된다
    (이걸 놓쳐 "두께 −18%" 오측이 한 번 올라갔다: [image_size](image_size_2026-08-21.md) ④)
  - 재개 지연 — `datasets.md` 의 "최대 3.2초" 는 **완전 가림 후 재개**이고,
    [publish_gap](publish_gap_2026-08-24.md) 의 13.4초는 **부분 가림 지속 시간**이다. 다른 양이다
  - **VRAM — 어느 기계에서 잰 값인지 밝힐 것.** 저장소의 VRAM 수치는 전부
    **RTX 4070 Ti 12,282 MiB · WSL2** 값이다. 그리고 **여유·비율은 기계를 옮기면
    수치가 아니라 정의가 깨진다** — DGX Spark(GB10)은 통합 메모리라 `nvidia-smi
    --query-gpu=memory.total` 이 `[N/A]` 를 내서 `12GB 대비 여유` · `남는 여유` ·
    `79.7%` 같은 **분모가 있는 양이 계산 자체가 안 된다.** 절대 사용량(② ④ ·
    SAM3 몫 · `+178 MiB`)은 값만 달라진다.
    → [shared_server](shared_server_2026-08-31.md) §1. **여유가 없는 것이 위험하다**
    는 저장소의 OOM 위험 모델이 여기 걸려 있다
- **"정답" 을 만들기 전에, 정답이 이미 저장소에 있는지 먼저 볼 것.**
  이 폴더는 *"기록은 있는데 안 읽혔다"* 때문에 만들어졌다(맨 위 인용문).
  **같은 사고가 정밀도 쪽에서 한 번 더 났다** — Isaac 씬 정답(카메라 z=1.408,
  벨트 상면 0.408, 작업물 200×55×55mm @ z=0.435)은 **2026-08-13 부터**
  [isaac_sim_connection](isaac_sim_connection_2026-08-13.md):98-118 에 있었는데,
  08-21~24 문서들이 그걸 못 보고 **depth 판독 두 개에서 "정답 57.3mm" 를
  파생해** 썼다. 그 파생 정답은 우리 측정과 같은 평면 편향을 공유해서
  "정답 +0.7mm" 로 맞아떨어졌다 — **검증이 순환한 것이다.** 씬 정답으로 재면
  두께는 **+3.2mm** 이고, 그 몫은 **평면 +2.45 / 상면 −0.5** 로 갈린다
  (**`dist_thresh=6mm` 값** — 현행 Isaac 3mm 가지는 **+1.97**, 평면 **+1.34**).
  → **정답을 측정에서 만들기 전에 `docs/` 를 grep 할 것.** 특히 씬 기하·
  카메라 파라미터·물체 치수는 이미 적혀 있을 가능성이 높다
- **std 는 재현성이 아니다 — 평면 오차는 std 에 구조적으로 안 나타난다.**
  `pipeline.py` 는 평면을 **검출이 처음 생긴 키프레임에서 한 번만** 맞추고
  영구 캐시한다(`_plane_tried`, 주석 "단발 추정 후 영구 캐시다").
  `geometry.py:190` 의 `pcd.segment_plane(dist_thresh, 3, 200)` 은 **시드 없는
  RANSAC** 이다. 따라서 **평면 오차는 한 실행 안에서 상수**이고, 프레임 간
  std(belt_plane 의 "두께 std 0~0.2mm", z-std, size_std, center_std)에는
  나타날 수 없다 — 그건 재현성이 아니라 **한 실행 안의 일관성**이다.
  → 그 표들은 **A/B 비교로는 유효, 절대 정밀도로는 인용 불가**다.
  - **허용치는 씬마다 다르다 — 그 씬의 측정된 평면 스프레드에서 유도할 것.**
    실측(repeats=21): **Isaac ±0.40mm · test4 ±2.98mm · test2 ±13mm 급.**
    Isaac 씬이면 **0.40mm 보다 작은 차이를 설정·해상도 효과로 해석하지 말 것**
    ([image_size](image_size_2026-08-21.md) 의 두께 0.1mm 차가 이 경우다 —
    **"같다" 가 아니라 "구분되지 않는다"**). 앞서 이 자리에 적혔던 "약 0.55mm
    (n=2 유도)" 는 **repeats=21 실측 0.40mm 로 대체**한다
  - **그렇다고 test2 에서 허용치를 13mm 로 키우면 안 된다.** 그건
    *"13mm 미만 차이는 해석하지 말라"* 가 되고, **그 씬에서 벨트 평면으로 할 수
    있는 말이 아무것도 없다**는 뜻이 된다. **13mm 씬은 허용치 문제가 아니라
    품질 게이트 문제다** — 허용치를 키우는 게 아니라 **그 평면을 기각하는 것이
    맞다.** 현행 `min_inlier_ratio` 는 그걸 못 잡는다(§4 열린 항목)
    - 단, **test2 의 ±13.1mm 가 평면 품질인지 지렛대인지는 아직 안 갈렸다** —
      `d` 는 카메라 원점까지 외삽한 값이라 지렛대가 붙는다(아래 "지표를 게이트로
      올리기 전에" 의 **관측량** 항목, `δd = δn·c_xy`). 갈리려면 `d` 가 아니라
      **물체 발밑 평면 높이**로 다시 재야 한다. *다만 그 씬 평면이 실제로 나쁘다는
      근거는 따로 있다* — 씬 **안**에서 `corr(inlier) = −0.562` 다. 즉 "품질
      게이트 문제" 라는 결론은 서 있고, **±13.1mm 라는 수치의 해석만 미결**이다
- **지표를 게이트로 올리기 전에 — 통계를 잘못 고르면 "괜찮다" 는 답이 나온다.**
  2026-08-25 belt_plane 작업에서 한꺼번에 나온 것들이다. 전부 같은 질문이다:
  *이 숫자가 정말 내가 알고 싶은 것을 재고 있나.* (출처:
  [belt_plane](belt_plane_2026-08-21.md) "잔차 지표는 왜 실패했나" ·
  "정정 — `min_inlier_ratio` 는 정보를 담고 있었다")
  - **씬을 섞어서 상관을 재지 말 것(pooling 함정) — 이 저장소는 두 번 밟았다.**
    잔차 RMS 의 `corr +0.652` 는 합성 격자에서 **씬 종류를 섞은** 값이고, 씬
    **안에서는** 전부 0 근처다(−0.048 / −0.025 / +0.071 / +0.317). 인위 혼합이면
    +0.942 까지 나온다. **같은 함정을 인라이어에서도 밟았다** — `corr +0.066`
    ("무상관") 역시 pooling 값이었고 씬 안에서는 **−0.562(test2) / −0.589(test4)**
    로 정보를 담고 있었다(§3-1 마지막 행). 더 나쁜 것은 **rms 의 pooling 함정을
    지적하는 커밋을 쓰면서 같은 표의 인라이어는 그대로 뒀다**는 점이다.
    → **한 지표를 pooling 때문에 기각·채택했으면 같은 표의 나머지 열도 전부 다시
    볼 것.** (두 건은 확신 강도가 다르다: 인라이어는 실기 4 bag 로 확정됐고,
    rms 의 +0.652 는 합성 29표본이라 원문도 *"이 함정일 가능성이 크다"* 로 적혀
    있다 — 그대로 인용할 것)
  - **비를 재기 전에 분자·분모의 공통 상수(척도 무관 항)를 먼저 뺄 것.** 폭 부족
    `−8.2`(672) vs `−5.2mm`(1008)로 *"입력px 로 환산하면 `(4.10 × 672) /
    (2.60 × 1008) = 1.051` 로 **불변** → 토큰 피복(+50%)과 무관 → 포화 기전
    **반증**"* 이라고 선언한 적이 있다. **틀렸다** — 그 안에 **화소중심 규약
    2.93mm** 가 들어 있고, 규약은 **원본 래스터**에서 나오므로 **두 arm 에 똑같이**
    걸린다. **척도 무관 항이 비를 1 쪽으로 끌어당긴 것**이다. 걷어내고 다시 재면
    **1.44 ~ 6.07×** 로 피복비(1.50×)를 **가운데 두고 걸치고**, 결론이
    *"반증"* 에서 **"이 자료로는 가를 수 없다"** 로 뒤집혔다(**그 비율 논증도
    이후 폐기됐다** — 아래 셋째 줄. 여기서 남는 것은 **비의 교훈**이지 그 숫자가
    아니다)([image_size](image_size_2026-08-21.md) "미결" 블록).
    → **비·배율·정규화 값을 근거로 쓰기 전에, 두 쪽에 공통으로 들어간 항이
    있는지부터 빼 볼 것.** 공통 항이 클수록 비는 **1 에 가까워 보이고**, 그건
    "차이가 없다" 가 아니라 **"이 비로는 차이가 안 보인다"** 다.
    - **그리고 공통 항을 뺀 뒤에도, 그 항이 구간으로만 알려져 있으면 구간
      전체를 훑을 것.** 위 정정에서 `[−0.3, +1.9]` 중 **`0.00 / 0.60 / 1.27 /
      1.90` 만 훑고 "전부 초선형" 이라고 적었는데, **교차점(1.50×)이 안 훑은
      끝에 있었다**(`−0.130`, 끝단 `−0.30` 에서는 1.44× 로 **선형 이하**).
      **부분구간으로 얻은 부등호를 구간 전체의 결론으로 쓰면 안 된다.**
      → 그리고 **결론이 입력 반올림보다 얇으면 그건 결론이 아니다** — 같은 건의
      운영점 초선형성이 원자료 `46.8 / 49.8` 의 **0.1mm 자리** 안에 통째로
      들어간다(`46.85/49.75` 면 정확히 1.500×). **그리고 이 비율 논증 자체가
      결국 폐기됐다** — 행수 세기가 그 미지 항을 닫아 버려 분모가 0 이 됐다.
      **구간을 훑기 전에 그 항을 직접 잴 길이 있는지부터 볼 것**이 더 짧은 답이었다
    - **그리고 "오차 0" 을 "항이 없다" 의 증거로 쓰지 말 것 — 상쇄일 수 있다.**
      같은 건에서 *"z=975 군은 depth 기하만으로 폭 **55.01mm**(정답 55.0) — depth
      는 폭을 잃지 않는다"* 를 확정으로 적었다. **아니었다** — 래스터 손실
      **−0.64** 와 corners 부풀림 **+0.65** 가 상쇄된 값이고, 그 군도 참 18.21px
      를 **18행**으로 잰다. → **판별법: 다른 조건에서 같은 분해가 닫히는지 볼 것.**
      z=945 에서는 같은 두 항이 −2.31 / +1.04 로 **−1.27** 을 내고, 그게 보고값
      53.73 과 맞는다 — **항이 없으면 두 조건 다 0 이어야 한다.** 이 착각 때문에
      "마스크가 폭을 깎는다" 가 한 번 더 살아남았다
    - **그리고 같은 양의 두 통계량이 다른 값을 낸다고 "다른 양" 으로 오진하지
      말 것 — 위상·조건 분포를 먼저 물을 것.** 같은 건에서 `−3.03mm`(합성
      **위상평균**)와 `−5.21mm`(실데이터 **고정 위상**)를 보고 *"서로 다른 오차
      예산을 섞었다"* 고 선언했고 **`geometry.py` docstring 에 경고까지 나갔다.**
      아니었다 — 0° 위상 오차는 **−1.79px(21%) / −0.79px(79%)** 둘뿐이고
      위상평균이 **−1.000px** 이라, **평균이 21:79 로 섞였을 뿐 같은 양**이다.
      → **두 수가 갈리면 "다른 양인가" 보다 "조건 분포가 다른가" 를 먼저 물을
      것.** 그리고 **한 조건에서 언 값을 그 조건의 상수로 읽지 말 것** — 관측
      5.24mm 는 21% 뽑기가 카메라 고정으로 얼어붙은 것이고, 79% 쪽이었다면
      2.31mm 였다([subpixel](subpixel_2026-08-25.md) §4-b).
      **경위도 교훈이다: 형제 팀 반박 → 계산 확인 → 양쪽 철회.** 오늘 팀 간
      교차 확인이 오진을 잡은 두 번째 사례다
  - **위 셋은 이웃이지만 같은 것이 아니다.** pooling 은 **표본**을 섞은 것,
    바로 위는 **항**을 섞은 것, 아래는 **게이트가 이미 제거한 표본**이 만든 상관
    이다. 셋 다 "섞으면 신호가 죽는다" 로 뭉뚱그리지 말 것 — **고치는 방법이
    다르다**(씬별로 재기 / 공통 항 빼기 / 현행 게이트 적용 후 재기)
  - **게이트가 이미 제거한 표본이 만든 상관 — 이건 pooling 이 아니다.**
    `시드산포`(반복 적합의 평면 산포)는 **씬 안에서** 재도 네 씬 전부 양수라
    (+0.211 / +0.332 / +0.593 / +0.275) **유일한 생존 후보**였다. 그런데 그 표는
    **문턱 이전 표본의 것**이었다 — 현행 `0.35` 가 자르는 프레임들이 상관을 통째로
    만들고 있었다. **pooling 을 걷어내도 안전하지 않다**는 뜻이고, 깨지는 방식이
    셋이다:
    - **게이트가 이미 자르는 소수 프레임이 상관을 만든다.** test4 는 문턱 0.30
      에서 **+0.812**(n=31) 인데, `0.35` 가 **추가로 자르는 4프레임**을 빼면
      **+0.085**(n=27) 다 — 이미 자르고 있는 것과 **완전한 중복**이라 추가 이득 0.
      그 4개 중 `kf0008` 은 **d_sd 6.4mm 인데 참오차가 0.0mm** 로, **산포가
      크면서 정확한 반례**다(나머지 셋: `kf0010` 21.4 / half 132.5mm · `kf0026`
      35.9 / 643.4 · `kf0027` 108.5 / 529.9)
    - **추정량 자체가 draw 마다 흔들린다.** test2 는 0.30/0.35 에서 **같은
      16프레임 · 같은 thr(6mm)** 인데, 그 **동일 집합**에 draw 만 바꾸면 corr 이
      **+0.008 ~ +0.695** 로 흔들린다(독립 6회가 +0.008~+0.654, 별도 재계산이
      **+0.695** — 재계산이 앞선 범위를 **벗어난 것 자체가 증거**다). 표본 선택이
      아니라 **추정량의 잡음**
    - **씬 안에서 순위가 역전되면 문턱이 아예 존재하지 않는다.** test4 의 최악
      `kf0011`(half **91.1mm**)의 지표값이 1.25~3.14 인데, 더 나은 `kf0017`
      (half 31.7mm)은 **5.28~9.13** 이다. **32mm 를 자르는 어떤 문턱도 91mm 를
      통과시킨다.** 상관계수가 아니라 **이 역전 하나로 게이트가 죽는다**
    나머지 두 씬에는 애초에 신호가 없다: **isaac 은 0.30/0.35 에서 40/40 동일**에
    corr **+0.079**, **test3 은 0.30 에서 11프레임 → 0.35 에서 0프레임**이다.
    그 11개는 half **37~289mm** 로 오차가 전부 큰데 corr 이 **−0.092** 다 — 순서를
    못 매긴다. **원래 표의 test3 `+0.275` 를 근거로 인용하지 말 것**: 그 표본은
    현 문턱에서 전부 사라졌고 남은 것들끼리는 음의 상관이다.
    → 상관을 보고 게이트를 만들기 전에 **(a) 현행 게이트가 이미 자르는 표본을
    빼도 남는지, (b) 같은 집합에 다시 뽑아도 재현되는지, (c) 씬 안 순위가
    단조인지** 를 볼 것. **(a) 가 이 건의 핵심이다 — 문턱 이전 표본으로 잰 상관은
    그 문턱의 추가 이득을 증명하지 못한다.**
  - **게이트에 요구할 것은 순위 능력(AUC)이 아니라 "통과 영역의 최대 오차" 다.**
    rms 의 pooled AUC 는 **0.88** 로 높은데 **최저 사분위(= "최상급 적합") 안에
    참오차 47.32mm** 가 있었다(등급 오염 168표본, 참 평면을 구성상 정확히 앎).
    반면 `절반불일치` 는 AUC 가 더 낮은데도 최저 사분위 **최대 2.82mm · >5mm
    0%** 다. 게이트가 할 수 있는 약속은 줄세우기가 아니라 **"통과시킨 것은 실제로
    좋다"** 하나뿐이다 — **AUC 를 근거로 게이트를 채택하지 말 것.**
  - **포화된 값을 씬 간에 나란히 놓지 말 것.** 잔차 RMS 는 인라이어가
    `|r| < dist_thresh` 안에만 있어 문턱에 묶인다(균등 분포 상한 `thr/√3`).
    폴백(3mm 우선 → 6mm)이 **씬마다 다른 문턱**을 고른 첫 표본은 포화도가
    **75~98%** 로 전부 상한 근처였다 — **씬 간 비교가 무효**다(6mm 로 고정해
    다시 쟀다). 인라이어도 절대 척도가 아니다: 기준선이 씬마다 다르다
    (isaac 0.51 / test2 0.43 / test4 0.42 / test3 0.25). **문턱 0.35 는 "모든
    씬에서 나쁜 것만 자르는" 값이지 절대 품질 척도가 아니다** — 씬 간에
    0.35 를 인용하지 말 것
  - **관측량을 잘못 고르면 딴 것을 잰다(지렛대).** 평면 `d` 는 카메라 원점까지
    외삽한 값이라 링 중심의 측방 오프셋만큼 기울기 지터가 증폭된다
    (`δd = δn·c_xy`; 합성 예측 0.10mm = 실측 0.10mm). 그래서 **test2 의 ±13.1mm
    는 평면 품질이 아니라 지렛대일 수 있다**(위 허용치 항목). 써야 할 변수는
    **물체 발밑 평면 높이** — OBB 두께가 실제로 소비하는 값이 그것이다.
    → **산포를 보고할 때 "무엇의 산포인가" 가 소비 지점과 같은지 볼 것**
  - **"자신 있게 틀린" 것은 잔차가 작다.** RANSAC 이 오염 구조에 붙으면 그
    부분집합에 아주 잘 맞는다 — 잔차 통계는 *"몇 점이 얼마나 잘 붙었나"* 만 보고
    *"이게 벨트냐"* 는 안 본다. 참값 있는 합성에서: `lip`(참오차 **15.51mm**)의
    rms 1.63mm 가 무해한 `ramp`(참오차 0.81mm)의 1.45mm 와 **0.18mm 차이**고,
    인라이어는 **유해한 쪽이 더 높다**(0.589 vs 0.313). → **적합 품질 지표로
    "맞는 대상에 붙었나" 를 물으면 안 된다.** 이게 rms 가 실기 test2/test4 를 못
    가른 이유이고, 표본 수 문제가 아니었다
  - **산포를 줄이는 후처리는 탐지하지 고치지 못한다.** K=21 median-of-K 의
    참오차가 단발과 같다(**12.65 vs 14.45mm**) — 지배 모드가 틀린 모드면 median
    은 그 틀린 답을 **결정론적으로** 낸다. **"median 으로 산포 5배 감소" 만 보고
    채택하면 조용히 틀린 값을 고정 출하한다.** 산포 감소는 채택 근거가 아니다
  - **"비용 때문에 못 쓴다" 로 기각하기 전에 판별력부터 볼 것.** 이 자리에는
    `시드산포` 를 두고 *"실행당 R회 적합이 필요해 단발 게이트로 못 쓴다"* 라고
    적혀 있었다. **틀렸다 — 비용은 반대 근거가 아니었다.** `fit_plane` 1회가
    3.91(isaac) / 5.37(test2) / 5.68(test4) / 6.12ms(test3), R=21 이 82.1 /
    112.9 / 119.2 / 128.5ms 인데 `_plane_tried` 때문에 **실행당 한 번**이라
    무시할 만하다. 진짜 기각 이유는 **판별력이 없다**는 것이다(바로 위 세 항목).
    덧붙여 이 지표는 씬 **안에서 사실상 상수**다(isaac 0.12~0.22 / test4
    0.69~1.4 / test2 1.95~4.32) — 프레임 판별이 아니라 **씬 블랙리스트**다. 문턱
    스윕에서 test4 를 확실히 낮추는 유일한 값 **0.80mm 는 test2 를 0/16 으로
    전멸**시켜, 저장소가 이미 거부한 `0.45`(test2 7/16)보다 나쁘다.
    **비용은 재면 끝나는 질문인데, 이 건은 비용이 무죄였고 지표가 유죄였다.**
    - **이름 주의 — `시드산포` 계열 수치가 세 곳에 다른 값으로 있다.** ①
      `fit_plane` docstring · 위 허용치 항목의 **±0.40 / ±2.98 / ±13.1mm**,
      ② R=21 실측 평면 `d` 표준편차 **0.18 / 0.94 / 2.58mm**,
      ③ [belt_plane](belt_plane_2026-08-21.md):252 표의 **`1.811d` / `3.405d`**.
      **씬 순서만 같을 뿐 서로 바꿔 쓸 수 없다** — 어느 표에서 온 값인지 같이
      적을 것(③ 의 `d` 접미 단위는 문서에 정의가 없다)
  - **결국 단발 게이트로 쓸 지표는 남지 않았다 — 그런데 결론은 "평면 품질은 못
    잰다" 가 아니라 "이 계측기의 분해능으로는 그 질문에 답할 수 없다" 이다.**
    위 판정들이 전부 기대는 오차 대리값 `half_disagree` 자체가 흔들린다:
    - 같은 프레임에서 **draw 마다 1.5~2배** (test2 `kf0000` 6회 재측정
      **20.96 ~ 37.62mm**)
    - **과대보고 배율 최대 10배** (합성 참 3.67mm → 보고 36.70mm)
    - **무게이트 최대오차가 재는 쪽마다 다르다** — test2 **82.3** / test4
      **91.1mm** 인데 `fit_plane` docstring 표는 **58 / 34mm** 다
    → **`598 → 34mm` 같은 수치를 절대값으로 인용하지 말 것.** "그 34mm 가 더
    주는가" 는 **이 계측기로는 답이 안 나오는 질문**이고, 그것 자체가 지금 문턱을
    더 조이지 말아야 할 이유다. 같은 수치가 §3-1 과 §4 에도 있으니 그쪽을 인용할
    때도 이 단서를 같이 달 것
  - **같은 우산의 마지막 변형: 문서·docstring 도 주장이다.** `compute_obb` 의
    "PCA-based ... via Open3D" 한 줄 때문에 틀린 가설이 하나 섰다. 사례와 결론은
    **§3-1 공통 교훈**에 있다(*"코드가 무엇을 하는지 문서가 아니라 소스에서
    확인할 것"*) — 여기서 반복하지 않는다
- **test3 의 "A 와 동일" 과 `check_accuracy` PASS 는 한 번의 추첨 결과다.**
  test3 은 지지면이 한 장의 평면이 아니라 링 인라이어 0.27 로 문턱(0.35) 아래라
  무구속 폴백을 탄다. 그런데 그 판정이 **시드 없는 RANSAC 200회**에 달려 있어
  **어느 실행에서 우연히 문턱을 넘으면 나쁜 평면이 영구 캐시된다** — 실측으로
  18회 실패 후 19번째에 "성공" 해 결과가 파탄난 적이 있다(pink block 발행
  336→168, size_std 6.7→104.9mm: [belt_plane](belt_plane_2026-08-21.md) test3 절).
  지금의 PASS 를 **"test3 은 안전하다" 로 읽지 말 것**
- **게이트를 켜고 뽑은 CSV 로는 그 게이트를 다시 정할 수 없다.**
  `scripts/run_offline.py:249` 가 `if not o.publishable: continue` 로 거르므로
  **게이트에 걸린 행이 CSV 에 아예 안 남는다** — 질문이 사는 자리가 잘려 나간다.
  `publish_score_min` 을 재검토하려면 반드시 **`--publish-score-min 0.0` 으로
  뽑고 `score` 컬럼으로 오프라인 재적용**할 것. 이건 아래 항목의 한 사례다
- **분석·계측 도구가 조용히 다른 것을 재고 있는지 먼저 의심할 것.**
  **손으로 테스트하면 통과하고 자동화에서만 실패하는 형태가 반복됐다** —
  2026-08-24 하루에만 네 번 났다:
  - `scripts/run_offline.py:184-188` — `--raw` 가 `geometry` 에서 함수를 따로
    import 해서 **A/B 가 무효**가 됐다(§4 "측정하면 답이 나오는 것" 의 침식 A/B 가
    이것 때문에 중단됐다). 이때 **호출 카운터를 증거로 삼은 것이 화근이었다** —
    패치한 함수가 수천 번 호출돼도 raw CSV 는 패치 안 된 경로였다.
    **증명은 카운터가 아니라 출력으로 한다**: (a) 설정마다 **반드시 달라져야 하는
    출력**이 실제로 달라지는가(`erode_px` 0/3/5 → `num_points`
    **590 / 557 / 483**), (b) **알려진 값을 재현하는 대조군**이 있는가
    (`erode 3` → 195.6 / 49.8 이 기존 측정 195.66 / 49.76 을 재현).
    다시 뽑은 침식 A/B 는 이 둘을 붙여서 유효해졌다
    ([image_size](image_size_2026-08-21.md):455-470)
  - `scripts/analyze_divergence.py` 가 상대 `sys.path` 로 **`build/` 사본을
    import** 하던 건 — 고친 소스가 아니라 옛 사본을 재고 있었다
  - `run.sh` 의 `cd` 뒤 `$(dirname "$0")` — **전송 설정이 조용히 누락**됐다
    (지금은 8행에서 저장소 루트로 `cd` 한 뒤 45행이 상대 경로로 source 한다)
  - 게이트를 켜고 뽑은 CSV 로 게이트를 정하려던 것 — 바로 위 항목
  - **`ROS_DOMAIN_ID` 를 격리하지 않고 bag 을 돌린 것** (2026-08-25, 실행 관측).
    **Isaac Sim 브리지가 켜져 있으면 노드가 구독하는 바로 그 토픽으로 다른
    해상도의 `camera_info` 를 쏜다** — bag 과 번갈아 도착해 서명이 매번 바뀌고,
    노드가 매 프레임 리셋해 **출력이 전량 폐기된다**(CSV 헤더 1줄). 에러도
    크래시도 없고 `camera_info changed ... tracker reset` 만 반복 찍힌다.
    → **bag 을 돌릴 때는 `ROS_DOMAIN_ID` 로 격리할 것.** `77` 로 격리하니
    발화 0회 · CSV 63행이었다. **Isaac 과 bag 이 둘 다 이 프로젝트의 입력이라
    Isaac 을 켜둔 채 bag 을 돌리는 사람은 반드시 만난다.** 사례는 §3-1
  같은 부류의 코드 결함도 하나 있었다: **지연 프레임 드롭 락아웃**(§2
  `LATE_DROP_STREAK_MAX`). 노드는 살아 있고 에러도 로그도 없이 드롭만 반복했다.
  → **"에러가 없다" 는 "맞게 돌고 있다" 가 아니다.**
- **오프라인 러너 FPS 는 성능 지표가 아니다** — mp4 인코딩·bag 디코딩이 28~30%.
  파이프라인 내부는 CSV 의 `proc_ms` 로 볼 것
- [belt_plane](belt_plane_2026-08-21.md) 의 "켜기 전에 필요한 검증" 절은
  **오전 판단 시점의 기록**이다. 같은 문서 "재평가" 절이 그것을 뒤집었다
